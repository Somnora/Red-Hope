# Build Log — The Red Hope

## 2026-07-07 — Session 32 (UX overhaul, part 2: Sims-style categorized build menu)

- **Built (`f-menu`):** the flat BUILD palette became **category tabs** (Power / Production / Life Support / Boring / Logistics…) plus a palette that shows only the selected category's buildings — the director's "organize it by category like the Sims." Switching tabs is a pure visibility flip (every button built once, gated by a category-bound visibility; no rebuild). The open tab glows; CamelCase names auto-space ("LifeSupport" → "Life Support").
- **Category source:** the new `FRHBuildingRow.Category` column, with a **code-derived fallback** (CanBore→Boring, CirculatesAir→LifeSupport, gen/storage→Power, coverage→Logistics, else Production) so the menu works NOW, before the DT re-sync lands the authored column (authored wins once synced).
- Pure presentation (RHCommandDeck only) → every headless suite identical by construction; compiles clean; 30 s boot smoke zero ensures. On-screen layout is the director's hand-play verdict.
- **Director signalled a broader goal:** "12 hours left, use all tokens improving the graphics and design, get as far along with game structure as fast as we can." UX arc part 3 (on-placement tooltips) still pending; the open directional call is whether to schedule a **Mars look/atmosphere pass** (lighting/sky/fog/post-process/color grading + material tuning — the brief's ochre-sky / warm-habitat-light Look), which is distinct from the standing gray-box-GEOMETRY rule (that rule is about meshes/sprites, not environment lighting). Surfaced to the director for a decision.

## 2026-07-07 — Session 31 (UX overhaul, part 1: the in-world action card — "click the machine, order it")

- **Director's directive:** take on the UX contextual-action overhaul (his #1 concrete pain). Via a previewed structured ask he chose the interaction model: **a card that pops up ABOVE the machine in the world** (not on the deck) — the most immersive of the three options.
- **BUILT + verified (`acdd412` data, `59ef202` code):**
  - **The in-world action card.** Clicking a building floats a card anchored above it in the 3D world: an `SConstraintCanvas` layer whose one child is positioned every Tick by projecting the building's world point (lifted 3.5 m) to screen via `ProjectWorldLocationToScreen` and dividing out the geometry DPI scale; slot `Alignment(0.5, 1.0)` hangs the card's bottom-centre on that point so it sits above the machine; off-screen / behind-camera collapses it.
  - **What the card shows:** the building's name, a **director-authored "what is this / how to use it" blurb**, its live state (the old inspect read-out, relocated here), and **action buttons for the verbs it affords with real costs**. For the Borer: **Bore Deeper** + **Excavate Cells** (with the CarveCell recipe's h/cell), which MOVE off the bottom bar onto the machine — decluttering the ORDER row to just world-placement orders (Dig/Survey/Map). The manual breaker moves here too. Every action issues a normal uplink order — the card never bypasses signal lag.
  - **Data:** new `FRHBuildingRow.Category` + `Blurb` columns (player-facing copy authored in `RH_Buildings.csv`; Category will drive the Sims-style menu next). Honest costs only — real time/power/materials from data; no invented "N crew" number, because the sim assigns robots from a shared pool, not per-task.
  - The old right-column inspect panel is removed (superseded). New `RH.Select <id>` raises a card without hunting the gray-box footprint with the cursor (hand-play + test aid).
- **Verification:** compiles clean; a 45 s live boot with a building selected (`RH.Select 1`) exercised the projection + every bound getter with **zero ensures/crashes**; all 9 headless suites exact + 10-sol baseline byte-identical (the struct field is inert to the sim). **Honest caveats:** (1) blurbs render blank until the next editor session's DT re-sync lands the new `Category`/`Blurb` columns — the card structure + actions work now, the copy populates on sync; (2) the card's on-screen POSITIONING is the one thing headless (`-nullrhi`, no Slate render) cannot prove — that's the director's hand-play verdict.
- **Remaining in the UX arc:** (2) the Sims-style CATEGORIZED build menu (using the new Category column); (3) richer on-placement tooltips. Both are follow-ups. Then M3 sovereignty per the roadmap.

## 2026-07-07 — Session 30 (director picks increments 2+3: the garden power fork + the water loop — both built + verified)

- **Director's roadmap call:** after the "Hope drives the colony" build, he chose the **garden power fork** and the **water loop** next (over the UX overhaul). Built as two separately-committed, headless-verified increments.
- **One architecture ruling that shaped both:** a naive `Regolith→Glass` Forge recipe would be UNSAFE — the Forge's fallback recipe selector (`FindRunnableRecipe(InputsOk)`, no shortage) picks the first slice-active runnable, and Regolith IS in the Forge hopper during the baseline, so Glass would hijack Struct and break every baseline. (`MakeShielding` is safe only because its `Struct:40;Ore:60` inputs are never staged there.) So Glass sources via a **GlassCrate manifest** (pool-add like SoilPallet, proven with the garden); local glass fabrication is a flagged follow-up needing its own kiln building.
- **Increment A — the garden power fork (`e5303c7`, save-neutral):**
  - Grow-lit **Garden** cells now cost battery energy (`GardenGrowLightWPerCell`/cell, debited in StepAgriculture BEFORE StepPower so the drain is band-consistent). All-or-nothing at colony scale (order-independent, legible): if the bank can't pay, the lights go **dark and the crop holds dormant** (not dead) — the director's "plan power before settlers" made mechanical. Grow-lit yield is steady/time-independent.
  - New **Greenhouse** room function (glass-glazed): plants from Soil+Seeds+**Glass**; near-free power but yield **rides the solar curve** (× dust) — dark at night, throttled in storms — and needs a **shallow floor** (`GreenhouseMinLevel`) for light. The low-energy, lower-yield alternative. `IsGardenFunction` unifies plant/forfeit across both.
  - Glass resource + GlassCrate manifest. Deck GARDEN line shows grow-light W + DARK. `Debug_InjectRoom/Resource/Manifest` + `Debug_SetBatteryWh` let the headless test exercise the new Greenhouse row before the DT is MCP-synced (whole-row extension of the SliceActive test-knob).
  - `-greenhouse` self-test all EXACT: growLightW=60, grow-lit steady 0.250 kg/0.25sol, blackout dark+dormant+still-rated, greenhouse needs glass (400→200), solar-gate (2 cells midday / 1 at night).
- **Increment B — the water loop (`fe064d6`, save v14):**
  - Colonists now **drink** `ColonistWaterKgPerSol`; **water joins the life-support contract** (no water = unsupported = the same abstract evac path — thirst is fatal, so ice matters).
  - **Greywater recycling** reclaims most of each draw (net loss only 15%) — but every recycle **degrades POTABILITY**: a single 0..1 pool-quality scalar that **decays linearly per sol** and is **restored only per-kg of fresh ice-melt** makeup (tracked at the MeltIce output seam). The decay and restore terms are LINEAR (k·dt / per-kg where the kg-total over a sol is band-identical), so they carry no instantaneous-pool dependence — the parity-relevant construction from the design-panel guardrail. (Honest caveat from the inline review below: the `[0,1]` CLAMP on the stored scalar is the one nonlinearity, so agent-vs-era can differ by a bounded epsilon when potability is pinned near a bound — a region with zero gameplay effect since the Hope penalty only bites below 0.6. The hard invariants are unaffected and verified.)
  - Below `WaterPotabilityFloor`, grim recycled water **saps Hope** (`HopeWaterPenalty` × how-far-below) — the **first scarcity-driven Hope input**, and the mechanical reason to **drill the ice caps**.
  - Save v14 (potability). Deck crew card + `RH.Hope` show Water kg + potability%. `Debug_AddFreshWater` restores potability in tests without scripting the whole ISRU chain. Colonist tests (`-crew`/`-rooms`/`-luxury`) provisioned Water.
  - `-water` self-test all EXACT: net draw 1.200 kg/sol, linear decay 0.160/2sol, penalty 3.360 at potability 0.432, fresh restore +0.196, v14 round-trip, thirst→evac.
- **Regression:** all 9 prior suites pass; the `-hopedrive` keystone smoother assertion stays `|err| 0.000` (its garden@tempo now reads `|err| 0.0002` because the long run develops a water penalty that drifts Hope during the window — self-consistent, expected). 10-sol baseline ledger byte-identical (power 50/20, Struct 400/300, deposit 40645). Fixed one latent issue: `FreshWaterThisStepKg` now resets on the zero-pop early-out too.
- **Adversarial review:** the review workflow hit the session limit (resets 3:10pm) with 0 finders completing, so I reviewed the combined diff INLINE against the four dimensions I'd defined. **Hard invariants verified directly:** save v14 positionally symmetric (`Ar << WaterPotability` right after the v13 block on both sides; load resets the derived fields); greywater reclaim counts MET draws only (no water from nothing); both light gates `continue` BEFORE the water draw (dark cell = truly dormant, no water spent); greenhouse depth gate sign correct (−1 lit, −2 dark). **Two LOW, gameplay-inert findings, documented not fixed** (design-decisions 2026-07-07k): (1) the potability clamp + grow-light bank-gate are instantaneous-state booleans → bounded epsilon agent-vs-era divergence only near a boundary (potability near 1.0 = zero Hope effect; grow-light at the knife-edge) — colonist-scale era softness has always been ~5% tolerance, not bit-exact, and the zero-pop baseline stays byte-identical; (2) re-zoning a planted Garden→Greenhouse skips the Glass glazing cost (no material duplication, a lateral move). Fixing either would add complexity for no gameplay gain.
- **Pending:** DT sync of the new rows/config next editor session (Glass resource, GlassCrate manifest, Greenhouse room, 9 config rows). **Save v14 invalidates prior on-disk saves** (expected).
- **State:** grow-lit-vs-greenhouse and the water/ice loop are live; the garden now competes for power, and clean water is a standing pressure with a first real Hope cost. Next per the roadmap: the UX contextual-action overhaul, then M3 sovereignty (trade routes → Solidarity Dilemma). Gate-D framing review still the standing mental-health hard-stop.

## 2026-07-07 — Session 29 (M3 design panel; director picks "Hope drives the colony"; Hope Band Spine + Work Tempo built + verified)

- **Design conversation (director's big-picture turn):** he asked what mechanical effects to add and offered a batch of half-baked ideas (rival-nation diplomacy/trade, Solidarity Dilemma, wonder/discovery, grow-lights-vs-glass, cryo-on-starvation, water loop, and a UX overhaul). Grounded the response in the brief: his diplomacy idea is **almost verbatim the brief's signature Phase-3 mechanic, the Solidarity Dilemma (§59-62)** — he rediscovered his own canon. Ran a 5-lens design panel (Workflow); 2 lenses completed before the session-limit reset (Hope-modulator, survival-economy), the rest triaged from my own analysis. Full synthesis recorded in design-decisions 2026-07-07j.
- **Director decision (structured ask):** build **"Hope drives the colony"** first — the Hope Band Spine + Work Tempo — over the UX overhaul and the garden-power fork (both queued next).
- **BUILT + verified headless (`db940f0`, save v13):** the answer to the brief's open question "what does Hope DO" (§76):
  - **HopeSmoothed** — the instantaneous Hope index low-passed (Tau=3 sols) into the colony's *mood*, so a rec room or a lost circulator moves the colony over sols, not per-frame. Uses the **exp(-dt/Tau) smoother** specifically so the agent band and the 60× era band converge to the IDENTICAL value — the era-parity property a naive per-step lerp would silently break. Verified `|err| 0.000` against the closed form in `-hopedrive`.
  - **Five hysteresis bands** (Failing/Strained/Steady/Thriving/Flourishing): rise past up-thresholds, fall past lower down-thresholds, so the band name never flickers. Saved (not re-derived) so save/load is exact-equivalent even mid-gap.
  - **Work Tempo** — `GetHumanWorkTempo()` 0.60..1.25, 1.00 at Hope 50, applied to COLONIST labor (garden yield today; lab/bench when they gain outputs). **Robots stay morale-immune** and tempo is **exactly 1.0 at zero population**, so every Phase-1 baseline and pre-crew regression is bit-identical. **Carrot-first, death-spiral guarded:** a strained colony is slow (floor 0.60), never dead — the mental-health directive made mechanical. The low-Hope *stick* (burnout/seat-rest) is deliberately NOT built yet, gated behind a hand-proven recovery.
  - Garden yield now scales with tempo (a thriving crew out-harvests a surviving one); Water draw stays a fixed physical cost. Deck HOPE line + `RH.Hope` show smoothed value + band name + tempo%.
  - `-hopedrive` self-test all exact: exp convergence (`|err| 0.000`), tempo transfer (1.0921 exact), garden-yield-scales-with-tempo (`|err| 0.0000`), band, save v13 round-trip. All seven prior suites (`-rooms/-garden/-luxury/-crew/-habitat/-vault/-borer`) + 10-sol baseline unchanged.
- **Pending:** DT_Config live sync of the 13 new Hope rows (CSV committed; code defaults match, so headless + in-editor both work until synced). **Save v13 invalidates prior on-disk saves** (expected — old versions refuse loudly); the `habitattest` dev-slot needs regenerating for live smokes.
- **State:** Hope is no longer an inert number — it drives the human economy, and it's the substrate the diplomacy/growth/discovery layers plug into. Next per director: the UX contextual-action overhaul, then the grow-lights-vs-glass garden-power fork; M3 sovereignty (trade routes → Solidarity Dilemma) is the larger arc. Gate-D framing review (all player-facing wording) still the standing mental-health hard-stop.

## 2026-07-07 — Session 28 (live DT sync via MCP; adversarial review of the fast-tracked code; pure-data headless sign-off)

- **DataTables synced to the live editor (MCP), then committed (`b2e7b4b`):** DT_Rooms — 7 rows to SliceActive=true (the six Gate-B functions + Garden for Gate C) plus two column corrections (Lab/Workstation NeedsPlumbing→false, Hallway NeedsFiltration→false) to match the CSV; DT_Config — +13 rows (7 Hope weights, 4 garden rates, 2 comforts); DT_Resources — Soil/Seeds/Luxury active; DT_ManifestItems — SoilPallet/SeedVault/LuxuryGoods effect text. **Every value read back with get_rows before trusting it.** Also folded in the long-queued **M1-c cleanup: removed the TEST event rows** (Storm_T sol 2, Flare_T sol 4) from DT_Events, leaving only canonical Storm_1 (sol 12) + Flare_1 (sol 17). All five assets saved via Save-All and committed BEFORE verification (insurance rule).
- **Adversarial review of the fast-tracked M2 Gate B/C/D code (a Workflow, ~1,600 lines over `7d43853..HEAD`):** six independent finder dimensions (adjacency geometry, Hope/jobs math, the agriculture loop, save v11/v12 round-trip, determinism/era-parity, presentation lifetime), each finding refuted-or-confirmed by a two-skeptic panel. **Result: zero confirmed defects.** Five dimensions clean; the single low finding (per-step display scratch `ComfortsSupplied`/`ProducingCells` not reset on load) was refuted by both skeptics and independently by me — the fields are unconditionally overwritten every step and the deck reads them from its own Slate tick, so no stale value reaches a frame, a saved byte, or the determinism/parity invariants. Per process discipline, a refuted finding gets no code change. (A bug in the review *harness* itself — an inner panel handed promises instead of thunks — was found and fixed mid-run.)
- **Pure-data headless sign-off (`a85b429`), after the director quit the editor:** base dylibs were already clean (no `-NNNN` hot-patches — no compile happened during the DT-sync editor window), so no relink was needed. Discovered the sync had introduced a real **test-vs-data drift**: `-rooms` asserted "Garden designation refused (dormant)", but Gate C had just made Garden active. **Fixed it the honest way — removed the in-memory `SliceActive` patches from `-rooms`/`-garden` so they now assert the flags straight from the committed DT (true pure-data verifiers that fail loudly on DT/CSV drift), and moved the dormant-refusal check to Smoking (genuinely still dormant).**
- **Full battery green on pure data (first run that reads the real DT_Config rows instead of code-default fallbacks):** `-rooms` (Hope 55.00/47.25/67.75/55.50/59.50 all exact, refusal `dormant=0`), `-garden` (plant/yield/dry/forfeit exact), `-luxury` (55→63→55), `-crew`, `-habitat`, `-vault`, `-borer` — all exact; the "DT/CSV out of sync" guard never fired, proving the synced flags drive the systems. 10-sol baseline ledger unchanged (power 50/20, deposit 40645) except **peak radiation now 1.000 not 3.000 — the intended Flare_T removal**, not a regression.
- **State:** M2 Gates A/B/C + D-abstract are built, self-tested, independently reviewed clean, data-synced, and verified end-to-end on pure data. Nothing blocked; no code owed. Remaining is the director's consolidated hand-play (zoning now runs off real data, no console knobs) and his Gate-D framing review (all player-facing wording still placeholder).

## 2026-07-07 — Session 27 (director fast-track: "get as much done as possible" — Gates B, C, and D-abstract built + verified in one sitting)

- **Director waived the Gate-A hand-play as a blocker** ("move ahead to gate B without the test... 1 day with Fable 5, get as much done as possible"); the hand-play folds into his next editor session. All work below verified headless + live-smoked instead.
- **Gate B1 — rooms + the Hope index (`d7e5b47` data, `c5b453b` code, save v11):**
  - Six room rows ACTIVE (LivingQuarters/Lab/Workstation/Dining/Cooking/Hallway); Garden/Smoking/Septic/WaterWorks stay dormant for their gates.
  - `DesignateRoom(Level, CellIndex, Room)`: zoning carved cells is free (function, not construction); allowed pre-rating (planning ahead); refuses dormant functions and uncarved cells. `Designate` rides the uplink like every order. **SpiralCell moved sim-side** — adjacency made cell geometry gameplay, so the sim owns the layout; presentation forwards to it.
  - **Adjacency calculus (habitat vision §4):** an EmitsTags room within Manhattan distance 2 of a RefusesTags room offends (Cooking/Garden vs LivingQuarters/Dining). At distance 1 nothing fits between (architectural, permanent). At distance 2, a **Hallway cell 4-adjacent to both ends + the floor's circulation running cancels it** — partition + filtration, the canonical cure.
  - **Jobs:** one seat per Lab/Workstation cell on a rated floor, assigned deterministically (shallowest floor, carve order, colonists by Id). Rebuilt every population step + on load, never saved (can't diverge).
  - **Hope (brief §5), pure derived read, all weights DT_Config:** base 50 + housing quality (15 max, LQ cells vs population) + functioning room types (MoraleWeight × 5, per type per rated floor) + jobs (3/seat) + vault milestone (5) − adjacency offenses (8/pair) − unsupported colonists (10 each), clamped 0..100. No sim state; effects on work speed/growth wait for later gates after the framing review.
  - `-rooms` self-test: hand-computed doubles all EXACT (baseline 55.00 → bad kitchen 47.25 with 2 offended pairs → hallway cure 67.75 → greed swap 55.50 — more beds, less Hope, a real tradeoff); uplink verb; save v11 round-trip incl. re-derived seats.
- **Gate B2 — zoning visible (`3bb2251`):** ZONE palette row above the command bar (from ForEachRoom, collapsed pre-M2); zone-paint mode (hover highlights the carved cell, click transmits one Designate, mode stays armed, right-click exits); carve tiles retint by function + flat name labels (state-diffed, zero steady-state work); colonists with posts hang around their zoned cells (JobPointCm by function). Live smoke clean.
- **Gate C — the garden (`5a0bd21` data, `d2f61dd` code, save v12):**
  - Soil/Seeds resources ACTIVE; SoilPallet → +1000 kg Soil, SeedVault → +200 kg Seeds — **the signature manifest gamble pays off.**
  - A Garden-zoned cell on a RATED floor **auto-plants** when the colony holds `GardenSoilKgPerCell` (250) + `GardenSeedsKgPerCell` (50): one pallet + one vault = a 4-cell garden. First planting fires "THE FIRST CROP IS PLANTED".
  - Planted cells yield `GardenFoodKgPerSolPerCell` (1.0) against a `GardenWaterKgPerSolPerCell` (4.0) draw — the garden is the colony's standing ISRU water demand; 3 cells ≈ break-even for a pod of 4. Dry taps pause yield (one alert per episode); rating loss makes the crop **dormant, never dead** (prevention framing); re-zoning a planted cell forfeits its soil, loudly.
  - Gardener job seats; planted tiles turn green (the only green on Mars); deck GARDEN line shows producing/planted + net kg/sol vs crew draw; `RH.Garden`. Save v12 (planted set, sorted for determinism).
  - `-garden` self-test EXACT (zoned-unfed waits; pallet lands → 2 plant, stocks 500/100; 2 sols → food +4.0, water −16.0; dry → 0 delta, still planted; v12 round-trip; re-zone forfeits). Live smoke: full arc (crew + pallet + vault land, cells zone via uplink, both plant). First live attempt failed to plant because habitattest's circulator is a PATCHED ChargePad — `RH.SetCirculates ChargePad 1` needed in live runs; the failure itself proved the rating gate.
- **Gate D abstract slice — comforts (`2a6fcb9` data, `9dced4c` code, no save bump):** Luxury resource ACTIVE, LuxuryGoods crate = 300 kg (~375 colonist-sols at `LuxuryKgPerColonistPerSol` 0.2). Colonists draw a luxury ration when stocked; **Hope lifts by `HopeLuxuryBonus` (8) × supplied fraction — pure bonus, absence never penalizes** (prevention framing; comforts are never part of the support contract). Deck comforts note; `-luxury` self-test exact (55.00 dry → 63.00 supplied, draw 0.8/sol for 4 → 55.00 drained, all 4 still supported). **All player-facing wording across B/C/D remains placeholder pending the director's Gate-D framing review — the standing mental-health gate.**
- **Regression discipline held throughout:** after every gate, all prior suites (`-rooms`/`-garden`/`-crew`/`-habitat`/`-vault`/`-borer`) green with values identical, 10-sol baseline ledger byte-identical (empty colonies no-op every new step by construction).
- **Pending next editor session (MCP):** DT sync — DT_Rooms (7 active flips), DT_Config (+13 rows: 7 Hope, 4 garden, 2 comforts), DT_Resources (Soil/Seeds/Luxury active), DT_ManifestItems (SoilPallet/SeedVault/LuxuryGoods effect text), plus the M1-c TEST event-row removal. Until then `RH.ActivateRoom` bridges live runs; headless tests patch rows in memory.
- **Agent-proposed balance flagged for director review:** all Hope weights, garden rates, luxury rates (numbers chosen for legible math, not tuned play).
- **Open design questions for the director:** (1) grow-light power draw for gardens (currently zero — flagged); (2) drinking-water/recycled-water psychology deferred until a water-recycling loop exists (WaterWorks room still dormant); (3) Hope currently has no mechanical effects — the brief's work-speed/growth/mutiny modulation needs its own ruling; (4) smoking area + tobacco crop wait on the framing review itself.

## 2026-07-07 — Session 26 (Gate A1 VERIFIED; Gate A2 built + smoked — the crew is visible)

- **Gate A1 verification (editor closed → clean relink, 6 s):** hot-patch dylibs removed, base relinked. **`-crew` all 7 assertions green:** housing gate refuses the pod at 0 beds; 4-cell vault certifies 4 beds; pod lands 4 colonists + 200 kg Food; 1-sol food math exact (197.5); save v10 round-trips (pop 4, first=Adeyemi); starvation marks 4 unsupported at 0.5 sol; all 4 evacuated at exactly 2.0 sols. **Regressions:** `-habitat`, `-vault`, `-borer` all green; 10-sol baseline ledger IDENTICAL to the known profile (gen 50 W / load 20 W / battery 2000/2000 / deposit 40645 kg / same radiation column) — the zero-pop no-op held by construction.
- **Live smoke (headless -game):** fresh colony + `RH.Deliver CrewPod` → "CREW POD: stays aboard (0 beds free, 4 needed)"; `RH.Load habitattest` (the rated vault) + second pod → 4 housed, +200 kg Food, `RH.Crew` roster all supported, `RH.Habitat` floor −1 LIVABLE. New **`RH.Deliver <Item>`** debug cheat (lands a manifest item's cargo effect instantly — the director's hand-play trigger for the arrival). Verified commit `7d43853`.
- **Gate A2 built (presentation only, no sim state, no save bump — `7426bd3`):**
  - **`URHCrewVisualizerSubsystem`** (new): one small suited figure per colonist — bone-white cylinder body + dark helmet with visor glint + flat name plate (same glyph-up convention as building labels). Pure listener; roster-diffed every tick.
  - **Arrival staged:** a colonist landing mid-session disembarks at the supply-ship pad, marches to the shaft head, and hard-cuts down to their home floor (the pit view's elevator language). A reload's existing crew seeds in place — no phantom marches.
  - **Life below:** residents wander the carved cells of their home floor (canonical `SpiralCell` layout, now shared from the colony visualizer); occasional **suited surface strolls** near the shaft head and back (the A2 spec's surface beat). Walk pace scales with sim speed (clamped ≤8× so era mode doesn't teleport them); paused colony = still figures.
  - **Distress read:** an unsupported colonist's suit tints hazard-amber (presentation of the A1 alert, still abstract — Gate-D reviews all framing).
  - **Deck crew card:** roster panel (name, floor, UNSUPPORTED + evac countdown) with population / free beds / Food kg and **~sols-of-food remaining** (computed from `ColonistFoodKgPerSol` via a new sim getter — no hardcoded balance). Width-pinned like the other four cards. Shows only once anyone has landed.
- **A2 verified:** compile clean (84 s, editor closed = base dylibs). 60 s live run: 4 figures spawn as residents in the vault on pod delivery, no ensures/errors. `-crew` rerun green post-relink.
- **Next:** director hand-play of the arrival beat (quota → manifest CrewPod → landing, or `RH.Deliver CrewPod` for the instant version), then Gate B (rooms/adjacency/Hope index) per `docs/m2-working-spec.md`.

## 2026-07-07 — Session 25 (M1-d CLOSED by director; M2 opens — Gate A1 "The Crew Arrives" built, verify pending relink)

- **M1-d closed:** director cleared progression to M2 ("UE is open, please move forward with the next task M2"). Close hygiene done via MCP (`9427723`): MinLivableCells=4 synced to live DT_Config; BP_VaultDemo removed from L_Slice; level + DT saved. **Phase 1 is complete.**
- **M2 sequencing decided (director, 2026-07-07h, via structured ask):** Gate A = **colonists as agents** ("the crew arrives" — people inhabit the vault before rooms/morale); mental-health-adjacent mechanics built with **abstract placeholders now, full framing review before ship** (Gate D). `docs/m2-working-spec.md` authored: A1 sim → A2 presentation → B rooms/adjacency/Hope → C garden/food → D framing review + luxuries; surface habs/vehicles sequenced after.
- **Gate A1 code (compiled clean, 201 s; verify pending clean relink):**
  - `FRHColonist` (Id, deterministic callsign, HomeLevel, bSupported, evac accumulator). **Save v10.**
  - **Arrival:** `CrewPod` manifest item (2400 kg, 4 colonists + 200 kg Food) — disembark ONLY into certified housing (rated floors × ColonistsPerCell beds, shallowest-first); no beds → pod returns aboard, loudly. The M1-d vault is the literal prerequisite for population.
  - **StepPopulation (both bands, no-op at zero pop):** each colonist breathes `ColonistO2KgPerSol` from their home floor's fill (the M1-d atmosphere's first consumer — a crowd can out-breathe a starved circulator) and eats `ColonistFoodKgPerSol` from pooled Food. Support edges alert with the missing leg named; sustained `ColonistEvacSols` unsupported → **evacuated to orbit** (abstract, prevention-framed; wording is Gate-D review fodder). Rating loss now cascades into stakes.
  - **Data live in DTs + CSVs (`8396629`):** Food resource ACTIVE (Provisions/Tank pool), CrewPod manifest row, 6 colonist config rows — verified via get_rows pre-save.
  - **Instruments:** `RH.Crew` roster, `RH.AddColonists`, CREW line in the deck readout (population/Food/UNSUPPORTED:N), commandlet **`-crew`** self-test (housing gate → vault certify → landing → 1-sol consumption math → save v10 round-trip → starve → evac).
- **Verification plan (next editor quit):** clean relink → `-crew` all-assertions + `-habitat`/`-vault`/`-borer`/10-sol regressions (zero-pop no-op keeps baselines identical by construction) → live smoke of the manifest→CrewPod→housing-gate arc.

## 2026-07-07 — Session 24 (director recording verdict: pit view v3 "distinct floors" + ground/night + HUD pin; adversarial self-review)

- **Watched the director's 35 s screen recording (frames extracted via ffmpeg).** Confirmed all three reported bugs from the footage: floor −1 carve tiles bleeding through at SURF (frames 12/15), the ground going dark/grey at night (frame 9, SOL 3 03%), and the right-anchored HUD drifting as the sparkline grew.
- **Pit view v3 — genuinely distinct floors:** `ApplyViewLevel` now hides a floor's visuals unless `BLevel == ViewLevel` (was: "surface always visible + open pit floor"). SURF = intact surface only; −N = that floor only. `IsUnderground()` replaces the old always-`-1` `PitLevel()`.
- **Ground never greys:** the real MarsGround plane is hidden ONLY while `IsUnderground()` (was: hidden whenever a shaft existed, replaced by gray-box skirt that read dark at night). The skirt/walls + shaft column are underground-only now.
- **Elevator ride:** `ARHStrategyPawn` smooths the focus-plane Z toward `TargetFocusZCm` (~1.5 s/floor) so descent reads as motion.
- **HUD pinned:** all four right-column panels (readout/fleet/inspect/known-ground) wrapped in `SNew(SBox).WidthOverride(ReadoutWidthPx=560)`, readout slot right-aligned — variable-width content can no longer shove the left edge.
- **Adversarial self-review (4-lens workflow) caught 3 regressions in the first cut, fixed before commit:** HIGH — descent clamped to total MaxDepth let you view an un-bored floor (surface hidden, ground showing, phantom pit); fixed by clamping `SetActiveLevel` to `GetShaftDepth()` and keying the ground hide off the view. MEDIUM — deposit/ship spawned while underground popped into the pit view; fixed born-hidden per view. LOW — shaft column born visible at SURF; fixed born-hidden. (Workflow's Verify phase died on a session limit; findings were triaged + verified inline from the completed Review phase.)
- **No sim code touched** (presentation + one controller clamp) → sim regressions unchanged by construction. Compile clean (33 s incremental). **Editor was open during compile → hot-patch dylib; needs a clean relink after UE closes, then the director's replay is the visual verdict.**

## 2026-07-07 — Session 23 (habitat minimum size — director ruling 2026-07-07f, verified headless)

- **Phase-1 exit now requires a 4-cell minimum vault** (`MinLivableCells`, DT_Config, data-driven; code default 4 matches CSV — live DT sync next editor session). A floor certifies Livable only at ≥ min cells AND pressurized + circulated. The chain still runs below the minimum (the floor pressurizes and drains the pool as you build toward it); only the rating + exit gate on size.
- **Legibility for the withheld exit:** one-shot "FLOOR N SEALED — X of 4 cells, carve K more" banner when a floor is atmospherically complete but undersized and no carve is queued (quiet during an active dig-out, speaks when you've stopped — the genuinely confusing moment). New `IsFloorSealedButSmall`; circulator inspection card shows `HABITAT: floor N, X/4 cells | O2 …` + status line; `RH.Habitat` shows `cells/min` + sealed/livable/suit-only.
- **Verified headless (`-habitat`, rewritten to prove the gate):** 2/4 cells fill 200/200 → `rated=0, sealed-but-small=1`, SEALED note fires once; carve to 4/4 → RATED LIVABLE + PHASE 1 EXIT; save/load v9 holds fill 400 / rated / vault; pool-dry → LOST at 392, 368 after 4 sols (the flap-fix's preserved genuine-failure path). `-vault` 7/7, `-borer` 4/4, 10-sol baseline identical. Two clean compiles.
- Resolves the open design question flagged at the Gate C commit.

## 2026-07-07 — Session 22 (director's screenshot verdict → pit view v2 + the full queued compile)

- **Director hand-played + screenshotted the slice view; five findings, all addressed this session:**
  1. **Floor tiles had gaps** → tiles are now exactly cell-sized (10 m, was 9.6) and top-flush with the floor plane — the dug base reads as one piece. Walls between rooms come later (his note), this is the base layer.
  2. **"No feeling of depth — it looks like we shaved a layer"** → **pit view v2**: the world is now one continuous lit scene with a hole dug in it. An instanced sand skirt (~300 m each way) replaces the ground plane once the shaft exists, dark-strata **walls line every open-to-rock edge from the surface down to the open floor**, and the carved tiles sit at the bottom — a perfect square hole in sand, seen from above, exactly the brief. Surface actors (buildings/robots/markers/ship) stay visible around the hole; the elevator changes which floor's pocket is open. Two ISM components, thousands-cheap, rebuilt only when the pit shape changes.
  3. **Lighting/exposure changed underground** → fixed by construction: the sun and scene never change (the old view hid the entire ground, so auto-exposure swung against the void). Windows/skylights as future light sources stay canon (§8).
  4. **"Didn't see the elevator move"** → the camera focus plane now RIDES between floors (~1.5 s smoothed descent) instead of cutting.
  5. **"Everything is square boxes"** → acknowledged; the bespoke building/robot art milestone stays deferred by the director's own earlier ruling ("when it makes more sense") — logged as reaffirmed for post-gameplay.
- **The whole queued batch compiled clean** (105 s): pit v2 + elevator ride + `Bore → -N` deck button + in-flight order holograms/double-place blocking + manual breakers (Switch OFF/ON) + Hold Fleet + the rating-flap fix (expansion ≠ crisis) + save v9.
- **All regressions green post-batch:** -borer 5/5 (H2 11.0 exact), -vault 7/7, -habitat exact incl. pool-dry loss (the genuine-failure path the flap-fix had to preserve), 10-sol baseline identical.
- Live -game smoke: bore-to-−2 arc with pit rebuild log lines, breakers + fleet hold exercised, zero ensures.

## 2026-07-07 — Session 21 (M1-d DT sync: the vault arc runs on PURE DATA; dormant human-layer DTs born)

- **DT sync (editor session, `822d923`):** Borer `CanBore=TRUE` + `H2BurnKgPerHour=1.5`; **AirFilter row live** (CirculatesAir=TRUE, 200 W, Struct:80, tax 30 kg, sheds late); ComputeModule tax 20 kg; **MakeShielding SliceActive=TRUE**; O2 config rows (fill 100 / leak 2 / rate 20). **DT_Rooms / DT_Compartments / DT_Doors imported from the staged CSVs** (10 rooms, 3 compartments, 2 doors — every row SliceActive FALSE, dormant per §9). All values verified byte-exact via get_rows before save.
- **The proof run:** the full vault arc headless with **ZERO in-memory patch knobs** — every prior `-borer`/`-habitat` verification used code-side row patches; this run was pure DT data end-to-end: Showcase's Borer (data CanBore) worked a real uplink Excavate designation as `CarveCell started on H2` (data 1.5 kg/h), the real AirFilter row built AT floor −1 through the uplink (underground: its 30 kg tax exempt), circulator filled from the pool → RATED LIVABLE → **PHASE 1 EXIT card**. Code was proven before; now the data is.
- **UI gap found + closed (code written, rides the next compile gate):** boring had no deck surface — Excavate had a button, the shaft didn't. New **`Bore → -N` button** (each click orders the trunk one floor deeper via the real uplink; label reads the next target; no-Borer rejection lands on the notice line). Until that compiles, `RH.Bore <depth>` is the console stand-in for hand-play.
- **TEST event rows (Storm_T sol 2 / Flare_T sol 4) deliberately kept** for the director's hand-play — it folds the outstanding M1-c weather-surface verdict (onset 1× snap, alert banner, storm sky, per-station cards) into the vault session naturally. Removal stays queued for M1-c/M1-d close.
- Next: **the director's vault hand-play** — the M1-d close gate.

## 2026-07-07 — Session 20f (M1-d Gate B close + Gate C: shielding tax, the trunk hauls, THE VAULT ARC LIVE, exit card, dormant schemas)

- **Shielding tax (`77abd89`):** `SurfaceShielding_kg` row column — surface bills gain `Shielding:N`, underground exempt (overburden). ALL construction math through `GetBuildCostFor` (preflight/site-haul/fabrication/deck card). Preflight principle sharpened: short stock WITH a completed producer online = an order that waits (refusal reads "no producer online"). **Demand-driven production:** construction shortage drives recipe preference (a Shielding-hungry site makes the Forge run `MakeShielding` over row order) + a store→producer feed leg (Struct+Ore → Forge hopper) — both scoped to active shortage, so steady-state logistics and the era parity bar never see them. MakeShielding ACTIVE; ComputeModule 20 kg / AirFilter 30 kg; bootstrap chain untaxed. Verified: commandlet (surface refused "Insufficient Shielding… no producer online", floor −1 ALLOWED) + live agent band (taxed ChargePad order → MakeShielding started→complete → site FINISHED). Known nit: one surplus Shielding batch can start while the first is in flight (becomes stock; damping later).
- **The trunk hauls (`cb0b052`):** `AreLevelsLinked` + `GetApproachPoint` — a robot serves a cross-level site from the SHAFT HEAD; the lift carries the last leg (robots surface-bound until a later gate). Site sourcing accepts linked floors; Build claims allow them; haul dropoff redirects. **THE FULL VAULT ARC RAN LIVE in one -game run: bore −2 → carve 2 cells → uplink-ordered ChargePad AT −1 built through the real economy → circulator → 200/200 kg O2 → FLOOR −1 RATED LIVABLE.**
- **Gate C — the exit card:** first-ever Livable rating fires **"PHASE 1 EXIT: THE VAULT — ready for a crew"** (banner alert + permanent readout line "THE VAULT IS LIVABLE - PHASE 1 EXIT READY"); fires once per colony, never retracts (a later rating loss is a crisis, not an un-achievement). Save **v8** (`bVaultRated`). Verified: -habitat run shows rating → exit card exactly once, loss announced without retraction.
- **Gate C — dormant human-layer schemas** (habitat-vision §9, the M0 dormant-row discipline): `FRHRoomRow` (function, EmitsTags/RefusesTags adjacency calculus, filtration/plumbing needs, MoraleWeight), `FRHCompartmentRow` (O2 buffer per cell, leak multiplier — the storm-breach retreat graph's nodes), `FRHDoorRow` (auto-seal on breach — the edges). CSVs staged (`RH_Rooms/RH_Compartments/RH_Doors.csv`, all rows SliceActive FALSE): 10 room functions incl. Hallway-as-partition and Smoking (tobacco luxury), 3 compartment classes, 2 door classes. **No sim consumer — M2 activates content, not schema.** DTs to be created next editor session.
- All regressions green (-borer 4/4, -vault 7/7 incl. the tax assertion, 10-sol baseline identical).
- **Next editor session (~10 min MCP):** DT sync — Borer CanBore/H2BurnKgPerHour, AirFilter row, MakeShielding SliceActive, ComputeModule/AirFilter tax values, DT_Rooms/DT_Compartments/DT_Doors creation; then eyes-on the slice view + THE DIRECTOR'S VAULT HAND-PLAY (the M1-d close gate).

## 2026-07-07 — Session 20e (M1-d Gate B core: the habitability chain, verified headless; save v7)

- **A carved floor is not a home** — the chain lands: bored (Gate A) → **shielded** (free from overburden, the M1-c radiation curve) → **oxygenated** (O2 fill mass = carved cells × `O2FillKgPerCell`, drawn from the colony pool and pushed down the trunk) → **circulated** (a completed, POWERED `CirculatesAir` station on the floor — the chain's last link; no circulator, no fill) → **rated LIVABLE**. Dig wide without the ISRU to match and it stays a spacesuit-only void — the underground ruling's core loop, now enforced.
- **Leakage is the standing tax:** every pressurized cell leaks `O2LeakKgPerCellPerSol`; a dry O2 pool drains the floor and the rating is LOST loudly (banner alert both ways; hysteresis rated-at-100%/lost-below-98% keeps healthy equilibrium from flapping). Deliberate tension flagged: the vault and the Q1 quota now compete for the same Oxygen pool.
- **AirFilter station** (CSV row, `CirculatesAir=TRUE`, 200 W, Struct:80, sheds late at priority 8 — life support): buildable on any reached floor; the habitat vision wants these THROUGHOUT in M2 — same schema. New row-struct column `CirculatesAir` (compile-gated; **DT sync next editor session** with the Borer columns).
- **StepHabitability** runs in BOTH bands (dimensionally honest like StepProduction — era-safe by construction). **Save v7** (per-floor O2 fill + ratings; v6 refuses). Config rows staged in CSV: `O2FillKgPerCell` 100 / `O2LeakKgPerCellPerSol` 2 / `O2FillRateKgPerHour` 20.
- **Legibility:** elevator floor cells gain a **●** when rated Livable; `RH.Habitat` logs every floor's chain state (carved / O2 fill vs required / circulation / rating).
- **Verified headless (`-run=RHSim -habitat`), every number exact:** no circulator → fill 0 after a sol; circulator placed on −1 → 200/200 kg and **RATED LIVABLE announced**; save/load v7 keeps fill + rating; pool drained → leak crossed 98% (196/200) with **loss announced**, 188 kg after 3 sols. `-borer`/`-vault`/10-sol regressions all green. Compile clean first try (56 s). Headless caveat as before: ChargePad row patched in memory for CirculatesAir (code proven; AirFilter DT row proves data in-editor).
- **Gate B remaining:** the shielding build tax (surface `Shielding:N` cost line + `MakeShielding` activation — next increment), then the Gate C exit card.

## 2026-07-07 — Session 20d (M1-d Gate A2 presentation: elevator + paint-to-size excavation + sliced-floor view, headless-smoked)

- **The elevator is live** (the M1-b shaft-strip stub grows up): each `SURF · −1 … −5` cell is a button; clicking rides the whole view to that floor — camera focus plane drops (`SetFocusZCm`), the colony mirror slices, orders target that Level. Cell states: bright teal = current floor, slate = trunk reaches it, dark = solid rock; carved-cell counts inline (`-1 ▦5`). `RH.Floor <L>` console equivalent.
- **Paint-to-size excavation** (the underground proposal's designation language): deck gains an EXCAVATE order button; click anchors a corner on the active subsurface floor, drag sizes the rect (snapped to 10 m carve cells, live cell-count + spoil-tonnage hint), release transmits the `Excavate` uplink order. Preflight paints red with "bore the shaft deeper first" when the floor isn't reached — the uplink re-checks regardless (the seam stays authoritative).
- **Sliced-floor view (gray-box ant farm):** descending the elevator hides the Mars ground plane (found by MI_MarsGround material — World Partition renames actors, labels are no handle), surface buildings, deposit markers, ship, and the robot ISM layer; the active floor shows its carved-cell tiles (10×10 m regolith-gray slabs), its buildings, and the dark amber-lit shaft column spanning the strata. One stratum at a time — floors above would roof the view in, deeper floors would bleed through unrendered rock.
- **Honest abstraction, flagged for the director:** the sim keeps carved cells as per-floor COUNTS (A1 model); tiles lay out in a deterministic square spiral around the shaft head — honest about how MUCH is carved, canonical (not player-painted) about WHERE. The painted rect sizes the order; it does not yet place the pocket. Spatial painted-position carving = a sim upgrade for a later gate if placement-in-pocket rules are wanted.
- **Level-aware interaction everywhere:** cursor ray meets the active floor's plane; build ghost/orders, dig lookups, click-to-inspect, and coverage discs all filter by the elevator's floor.
- **Verified headless (-game -nullrhi smoke):** BoreNow −2 → Excavate 5 → elevator −1 → 0 → −2 round trip ran the full slice path (ground found, zero ensures/crashes); `-borer`/`-vault`/10-sol regressions all green (H2 11.0 exact, vault 6/6, baseline identical). Compiles clean (25 s + 11 s). **Visual verdict needs the editor** — next session: DT Borer columns (CanBore=TRUE, H2BurnKgPerHour=1.5) + eyes-on the slice view + director hand-feel.

## 2026-07-07 — Session 20c (M1-d Gate A2 sim layer: Borer designations + hydrogen fuel + save v6, verified headless)

- **Data first (editor session, insurance-committed `958cd9a`):** Borer building live in DT_Buildings (3×3, 500 W, Struct:180, priority 2 — sheds early); `BoreFloor` (12 sol-h → one floor descent, Regolith:1200 spoil) and `CarveCell` (3 sol-h → one 10×10 cell, Regolith:1200) in DT_Recipes; ShaftSpoilKgPerFloor/SpoilKgPerCell synced to DT_Config. Designation recipes are inert to the auto-start search by construction (empty inputs + non-extractor).
- **Designations through the real uplink:** `Bore`/`Excavate` are uplink verbs (lag, rejections: "No Borer online", "bore the shaft deeper first"). The Borer works the queues through the EXISTING batch integrator — power, shedding, era parity all inherited. Bore-before-carve, carve shallowest-first over sorted keys (determinism); carve committed at batch start, world change applied at completion via `PendingBoreWork` (BuildingId → work item).
- **Spoil is real economy:** each batch drops 1200 kg Regolith at the Borer; existing hauling/era-logistics move it to the Forge. Verified: regolith flowed to stores during the bore phase.
- **Hydrogen fuel (`H2BurnKgPerHour`, row schema):** a batch buys its whole fuel up-front from Hydrogen stock when available and then draws idle grid power only AND **runs straight through shedding** (`bBatchOnH2` exempts the stall; nothing new starts while shed). The Electrolyzer's byproduct becomes strategic.
- **Save v6:** designations (`BoreTargetDepth`, `CarveQueue`), in-flight work (`PendingBoreWork`), and `bBatchOnH2` serialize; v5 refuses.
- **Legibility:** Borer inspection card reads standing orders (shaft −N of −M ordered, per-floor carved/queued, "running on H2 — works through shedding"); `RH.Status` shaft line; `RH.Bore`/`RH.Excavate` (uplink) vs `RH.BoreNow`/`RH.ExcavateNow` (debug-instant).
- **Verified headless (`-run=RHSim -borer`), all assertions green:** bore designation −2 worked as two 12-h batches across 2 sols; carve 2+2 cells; **mid-flight save/load v6 resumed an in-air CarveCell batch and completed it** (carved 3→4 post-reload); H2 phase exact (20 kg → 11.0 after 2 batches × 4.5, batches logged "started on H2"). `-vault` A1 regression all-green; 10-sol baseline figure-identical. Compile clean first try (48 s).
- **Known gap (deliberate):** live DT_Buildings predates the `CanBore`/`H2BurnKgPerHour` columns — the `-borer` test patches the loaded row in memory to CSV-staged values (proves the CODE). **Next editor session: set Borer CanBore=TRUE, H2BurnKgPerHour=1.5 via MCP + in-editor smoke (proves the DATA).** Then A2 presentation: paint-to-size excavation UI, sliced-floor camera + elevator selector + shaft HUD.

## 2026-07-07 — Session 20b (M1-d Gate A1: the shaft/excavation model + save v5, verified headless)

- **Director green-lit M1-d Gate A** (`docs/m1d-working-spec.md`). Built the first reviewable increment, **A1 — the sim foundation** (code-only, no editor needed; the Borer DataTable row + paint-to-size UI + sliced camera are A2):
  - **Shaft/excavation state:** `ShaftDepth` (floors the trunk reaches), `ShaftHeadCm`, `FloorCarvedCells` (per-level carved 10×10 count), `SpoilPileKg`. `ExtendShaft(depth)` emits `ShaftSpoilKgPerFloor` per bored floor; `ExcavateFloor(level, cells)` emits `SpoilKgPerCell`, refusing unreached floors. Config rows `ShaftSpoilKgPerFloor`/`SpoilKgPerCell` (1200/1200, CSV; **DT sync in A2's editor session** — code defaults match).
  - **Level becomes buildable:** the hard `if (Level != 0)` placement refusal is replaced by trunk connectivity — `CanPlaceBuilding` allows `Level<0` iff `IsLevelConnected` (bore reached it), and the shaft is the grid node + coverage on a reached subsurface floor (link/coverage checks short-circuit). The build command already threaded `Cmd.Level`, so the seam was one function. Surface (`Level==0`) path provably unchanged.
  - **Save v5:** shaft depth + head + carved map + spoil serialized; v4 refuses loudly.
  - **Instruments:** `RH.Bore` / `RH.Excavate` cheats, `RH.Build … [Level]`, shaft line in `RH.Status`; RHSim commandlet `-vault` self-test.
- **Verified headless (`-run=RHSim -vault`), every assertion green:** bore −2 → 2400 kg spoil; excavate −1×4 → 7200 kg, carved 4; unreached −4/−3 refused "bore deeper"; connectivity −1/−2 yes, −3 no; **ChargePad @−1 ALLOWED** (full coverage/footprint/cost chain), @−3 refused; radiation 1.0→0.05→0.0025; **save v5 round-trip** restored depth 2 / carved 4 / spoil 7200 after dirtying. 10-sol surface-arc regression identical to baseline (gen 50, Struct 400/300). Compile clean (66 s).
- **Next — A2 (needs an editor session):** Borer building DT row + spoil/fuel recipes synced; hydrogen fuel draw; paint-to-size excavation UI; the sliced-ant-farm camera + elevator selector + shaft-section HUD widget; spoil→Forge hauling.

## 2026-07-07 — Session 20 (StormWearMul live in DT; radiation plumbing; shielding tax deferred to M1-d)

- **Editor task cleared:** `StormWearMul = 2.0` added to the live DT_Config (was code-default-only since Session 19). Radiation config rows landed alongside it: `RadiationSurface = 1.0`, `RadiationPerLevelMul = 0.05`. Asset saved + insurance-committed before the compile gate (`5073816`, per the Content-trash rule).
- **Radiation plumbing (M1-c):** `GetRadiationAtLevel(Level)` — surface index 1.0, each floor of overburden ×0.05 (≈20× shielding/floor); `GetRadiationNow(Level)` — a live solar flare multiplies the *surface* index by its severity while subsurface stays shielded. Config-driven (GetConfigScalar, defaults match CSV).
- **The flare now has a per-station tell.** Its only prior surface was the alert banner + a silent wear ×3. An exposed station's inspection card now reads `SOLAR FLARE: radiation ×3 (exposed at surface; shielded underground)` — the symmetric partner to the storm's `solar output at 30%` line, and it plants the M1-d underground payoff. `RH.Radiation` cheat dumps the per-floor curve; the RHSim commandlet now prints the shielding curve + peak surface exposure over a run.
- **Shielding build tax DEFERRED to M1-d (design ruling this session):** every building is Level 0 today (no subsurface until the M1-d shaft), so a surface tax would raise all costs uniformly, break the M0-c regression arc, and create zero tradeoff — there's no underground alternative to shield toward. It becomes a real decision only in M1-d paired with the vault. Moving it there tightens M1-c's remaining scope to: radiation plumbing (done) + the director's weather-surface hand-play.
- **Verified headless (RHSim commandlet):** peak surface radiation over a 6-sol run = **3.000** (the sol-4 test flare, severity 3.0, plumbs through `GetRadiationNow`); clear-sky surface = 1.000; overburden curve 1.0 → 0.05 → 0.0025 down the shaft. Two compiles clean (54 s full, 10 s incremental).
- **Still open for the director:** RC-M vs no-robot-repairs ruling (gates M1-d schema); the weather/alert-surface hand-play (editor build is current); TEST event-row removal at M1-c close.

## 2026-07-07 — Session 19 (director weather/arrival rulings implemented + verified headless)

- **Rulings from the M1-c hand-play debrief (design-decisions 2026-07-07b), all implemented this session:**
  - **Onset snaps to 1×:** any speed drops to 1× the step a storm/flare begins (StepEventEdges, both bands) with a banner-weight alert ("batten down", wear warning included). Player re-speeds at will.
  - **Era rule amended:** "sleep through the siege, never the onset, never a flare" — CanEnterEraMode now allows era during a steady-state DustStorm, still refuses active flares and any imminent onset.
  - **Storm wear:** robots working through a dust storm wear at `StormWearMul` (2×, new config row; code default matches CSV until the DT row is added next editor session).
  - **Ship countdown:** T-2 / T-1 sol alerts on the supply ship (stage resets per launch; re-derives on load). Crew ships inherit the seam in M2.
  - **Alert channel:** new sim `OnAlert` delegate → visualizer 15-s hold → deck banner (top center, under the event banner). Era refusal moved from the missable notice line to this channel — the director's "nothing happened" finding.
  - **Per-station storm legibility:** a generator's inspection card now reads "DUST STORM: solar output at 30% of clear sky"; SHED line explains the deficit.
- **Scheduled, not implemented:** warehouse/garage shelter (M2, schema headroom M1-d); flares-as-electronic-faults repaired by humans (M2) — flare wear ×3 stands as placeholder; **open ruling flagged:** does no-robot-repairs retire the RC-M mechanical loop or apply only to electronic faults?
- **Habitat vision §9 milestone mapping APPROVED by the director** (M1-d births room/compartment/door/filtration schemas dormant; M2 activates the human layer); rulings appended to the vision doc as §10.
- **Verified headless, two runs:** (1) 8× colony hit the sol-2 test storm → `DUST STORM ONSET … Speed set to 1x` same step. (2) Loaded mid-storm, `RH.SetSpeed 60` → **engaged** (zero DustStorm refusals — the amended rule live), storm-end alert fired from inside era, then `ERA REFUSED: SolarFlare imminent` auto-drop, then the flare onset alert. Compile clean first try (64 s).

## 2026-07-07 — Session 18b (director hand-play of M1-c + the habitat vision)

- **Director hand-played the storm/flare build.** Two findings, both queued for the next compile gate:
  1. **Era refusal is invisible in anger:** pressing 60× during the storm/flare read as "nothing happened" — the refusal notice goes to the small orange line and was missed. Fix queued: unmissable refusal treatment (banner-weight, PAUSED-style). Design question surfaced to the director alongside: allow era during *steady-state* storms (onset always experienced at agent speed) while flares stay refused — proposed rule "you can sleep through the siege, never through the onset, never through a flare."
  2. **Storm impact on stations is not legible at the station:** the top banner alone carries the story (missable); the inspection card says nothing. Fix queued: card shows storm-derated generation and shed-reason; solar arrays' card line reads "gen X W (storm: 30%)".
- **The habitat vision landed as canon direction** — captured in full in `docs/habitat-vision.md` (modular lego habs + paint-to-size shells/hallways, room types incl. luxury crops/tobacco, filtration/plumbing/septic adjacency-as-gameplay with morale/sickness stakes, pressurized-door compartments for storm-breach retreat, interior visibility, surface↔underground connection, domes/windows trading shielding for light+morale, suits, vehicles). Milestone mapping proposed in §9: M1-d births the schemas (rooms, compartments, doors, filtration coverage — dormant), M2 activates the human layer. Awaiting director OK on the mapping.
- Territory question answered in-session: coverage grows by Pylon chains (80 m link, already in game since M0-a).

## 2026-07-07 — Session 18 (M1-c Gate A VERIFIED in-engine: the storm/flare arc end-to-end)

- **DT_Events created via MCP** (FRHEventRow schema; canon rows Storm_1 sols 12–15 ×0.3 + Flare_1 sol 17 ×3, plus TEST rows Storm_T sol 2 / Flare_T sol 4 so verification and hand-play reach weather in minutes — TEST rows removed at M1-c close). `events=4` confirmed at boot.
- **BP_M1cTest headless run (~6.5 min at 8×), all pass:**
  - `ERA REFUSED: DustStorm imminent - holding 8x` then `ERA REFUSED: SolarFlare imminent` — both auto-drops fired at exactly the onset horizon, restoring the agent speed (never dumping to 1×).
  - Mid-storm status: `EVENT: DustStorm until sol 3.0 (severity 0.30; dust factor now 0.30)`, midday gen 309 W vs 1199 W clear-sky midday two sols later — the collapse is the data row. Colony rode the bank (6000 → 2838 Wh overnight) and came out clean.
  - Flare: `EVENT: SolarFlare (severity 3.00; dust factor 1.00)` — flares don't dim, they irradiate; working robots' wear visibly accelerated through the flare window (RC-E ~+3.0 wear in 0.3 sol vs ~+1 baseline), idle robots untouched. The tax reads exactly as designed: exertion under an angry sky costs triple.
- Harness retired from L_Slice by commit (asset stays in Core). Director hand-play of the M1-c surfaces is the remaining stage gate: storm banner + darkened sun at sol 2, uplink-panel order cancel, sparklines through the storm.

## 2026-07-07 — Session 17 (M1-c Gate A verified by paired-run iteration; Gate B flare tax + Gate C UI written)

- **The paired-run harness earned its keep — four era-leg iterations, each fix a physical constraint, never a tuning knob** (same sol-3 save; agent 8× vs era 60× to sol 9; `RH.Ledger` diff):
  1. Baseline: era **under**-extracted 8.4% and held 58% less raw regolith — the Session 9 "digs only to feed demanding consumers" limitation, measured. Fix: leftover era dig budget flows to stores (the agent band's pile→hauler→stockpile loop, integrated).
  2. Then era **over**-dug (deposit −10.7%): it ran 24/7 while agent robots halt after dark on an empty bank. Fix: the **night gate** — no sun + no stored energy = no work.
  3. Then still ~5% hot: daytime charge trips (25%→90% docked hours) weren't modeled. Fix: **derived duty factor** — work-hours vs charge-hours per cycle, every term a data row (RC-E ≈ 0.81). Not a magic constant.
  4. **Final: extraction 0.4%, stores 4.0%, battery ~0 — bar met.** `Struct +7.4%` is the one line over: root-caused to **brownout-throttle oscillation** — the power-starved test colony's Forge duty-cycles at sub-step granularity under shed oscillation (Session 9's observation), which a 1-min integrator cannot see; era's cleaner cycling over-produces. On a healthy grid this term collapses. Logged as a measured, understood deviation; harness (staged saves + RH.Ledger) stays the regression net. Known deviations by design: SpareParts (era models no RC-M repairs), robot wear static in era band.
- **Era-honesty fixes shipped en route** (commit `e84d3b3` + this batch): StepProduction time-budget integrator (overshoot carries across batch boundaries — kills whole-step rounding at era dt); clock accumulator debt carries across frames (the 43 sim-s era→agent dump, gone; only >120 sim-s backlog drops, loudly); `RH.Ledger` instrument.
- **Events live:** `FRHEventRow`/DT_Events path + RH_Events.csv (Storm_1 sols 12–15 solar×0.3; Flare_1 sol 17 wear×3); StepPower applies the storm dust factor; era refuses during events and within one era step of onset; RH.Status prints the active event.
- **Gate B written:** flare wear tax in AccrueWear (active flare multiplies exertion wear; docked-at-pad is NOT shelter until the M1-d vault — deliberate). Radiation config rows staged in CSV. Shielding build tax deferred to next session (changes CSVs + harness together).
- **Gate C written + compiled:** event banner (amber storm / red flare, top center); **cancellable uplink queue panel** (orders in flight with live Δ countdowns, × aborts before execution — M0 spec §8 delivered; the signal can beat the click, which is the game); **power strip-chart** (last 3 sols gen/load/bank as block-glyph sparklines in the readout); ComputeModule completion hook (lag 45→20 s, min() so ComputeCore tier 2 never regresses); storm sky presentation (Dust MPC scalar + sun intensity scaling with generation).
- Awaiting next editor session: DT_Events asset creation (struct now compiled), BP_M1cTest storm harness, director hand-play of the M1-c surfaces.

## 2026-07-07 — Session 16 (M1-b CLOSED by director hand-play; survey map; M1-c Gate A code)

- **M1-b closed on the director's hand-played verdict** ("everything feels pretty good… we are getting somewhere"): survey → Ice_A discovery, inspection card, fleet panel, PAUSED banner all exercised by hand. M1-c approved as specced.
- **Surveyed-land map (director request, same session):** sim-owned survey history (point/radius/sol/found; **save v4**); deck `Map` toggle → in-world coverage circles (teal = found, gray = empty) + Known Ground panel (every discovered deposit: type/tonnage/coords/dig status); empty surveys announce on the notice line. Verified headless (one hit + one empty survey, both recorded); committed `6e11507`.
- **M1-c Gate A written + compiled clean (verification in progress):**
  - `FRHEventRow` + `DT_Events` path (absent table = clear skies, graceful); `RH_Events.csv` authored (Storm_1: sols 12–15 solar×0.3; Flare_1: sol 17 half-sol ×3 wear — flare effects are Gate B).
  - **DustFactor finally moves:** `StepPower` multiplies solar by the active storm's Severity (via `GetDustFactorNow`); `RH.Status` prints the active event.
  - **Era auto-drop:** `CanEnterEraMode` refuses during any active event and within one era step of onset ("DustStorm imminent").
  - **Era-honesty fix 1 (the 8.8% divergence):** `StepProduction` rewritten as a time-budget integrator — batch progress, completion, and next-batch start all inside one step with overshoot carried, so a 2 h batch no longer costs whole-step rounding at era dt. Same function both bands (dimensional honesty preserved); agent-band change is sub-step-scale only.
  - **Era-honesty fix 2 (the 43 sim-s dump):** the clock's beyond-cap accumulator debt now CARRIES across frames (fast-forward at the per-frame cap) instead of being dropped on era→agent transitions and load hitches; only a runaway backlog past 120 sim-s drops, loudly.
  - `RH.Ledger` instrument: parseable per-stock lines (solids/fluids/deposits) — the paired-run harness's diff format.
- **Paired-run divergence harness running headless** (same sol-3 save → agent 8× vs era 60× legs → ledger diff; ≤5% bar): results next entry. Known deviation: 6-sol window pre-quota (era refuses near an unmet deadline — by design); the full 10-sol post-quota harness lands with BP_M1cTest next editor session, along with the DT_Events asset (row struct compiled this build).

## 2026-07-07 — Session 15 (M1-b Gate C: the player's eyes — compiled, awaiting hand-play)

- **All presentation, zero sim changes** (per spec §5 + the Gate C findings from the director's first hand-played run):
  - **Inspection card** (the panel promised to the director): click any building → card on the right column — name/id/position; construction state with per-material delivered/outstanding lines ("material Struct: 80 / 250 kg (hauling)"); ONLINE/SHED; live batch line with recipe + hours left; def power profile (gen/draw/idle/store); attached deposit with tonnage; hopper in/out contents. Cyan frame drawn on the selected building; click ground / right-click / Esc dismisses; selection survives ticks but clears if the building vanishes (load).
  - **Fleet panel** (right column, always on): per-robot line — def, state (idle/DIG/HAUL/BUILD/CHARGE/SURVEY/REPAIR), battery %, wear with degraded/HALTED/stranded flags; header shows fleet count + SpareParts stock. The wear system's legibility surface.
  - **Notice-channel unification:** `ShowToast`/GEngine debug retired from gameplay entirely. Three deck channels now: orange notice (rejections + discoveries, visualizer), cyan **confirm** line (order transmitted / save / load, PC-owned, 6 s real-time hold), and the **mode hint** — the placement/dig/survey prompt moved from invisible top-left debug text to a bottom-center bar above the palette, colored by validity.
  - **PAUSED banner:** bottom-center, dark red, "— PAUSED — Space or a speed key to resume" whenever speed is 0 — the Gate C session-start stall can't recur.
  - **Survey button** (ORDER group): click → cursor carries the scout's true reveal circle (radius read from the Scout row's WorkRate — preview honesty, same number CompleteSurvey uses) → click transmits the uplink Survey order. The discovery mechanic is now hand-playable, not console-only.
- **Compile:** clean first try, 55 s. Headless smoke run: deck constructs under nullrhi, sim boots, zero ensures/fatals.
- Remaining to close M1-b: remove BP_M1bTest_GateB from L_Slice (next editor session), then the stage-end **hand-played director run** — survey the NE ridge by hand, watch Ice_A appear, click buildings mid-run, read the fleet panel while robots wear.

## 2026-07-06/07 — Session 14 (M1-b Gate B: fleet reality — verified headless)

- **Scope landed (per spec §4 + charge etiquette):** wear accrues with exertion (any sub-step above idle draw, `WearPerActiveSol` from DT_Robots, clamped at halt); work rate degrades linearly 50→100 (`GetWearWorkMul`); at 100 the brain processor's halt gate stops ticking the robot entirely — repairable, never destroyed. RC-M **Repair**: claims the most-worn robot ≥ degrade threshold (parts in stock, self excluded, one claim per target), re-tracks a moving patient, spends SpareParts at 25 wear/part instantly on arrival. RC-S **Survey**: `RH.Survey <Xm> <Ym>` crosses the uplink → board task → scout drives out → deposits within `WorkRate` (60 m) reveal, marker + notice on discovery. **Discovery honesty end-to-end:** `SurfaceVisible=FALSE` rows (Ice_A/Ore_B/Ice_B — flags waiting in the CSV since M0) are invisible to FindDepositNear, dig designation ("Unknown deposit", no information leak), drill auto-attach, and the visualizer. **Charge-pad queue:** one umbilical — only the queue head draws; slot taken at docking so a robot that dies en route can't block the pad; legacy brain bypasses (frozen M0-c baseline). Save v3 (discovery + survey targets; wear already rode v1). Era mode refuses during field ops. New instruments: `RH.Survey`, `RH.Wear <0-100>`, `RH.Fleet`. StarterSpareParts=6 finally stocked at boot.
- **Deviation from spec, logged:** survey/repair are claim branches inside the StateTree's Work task (the dig/haul/build pattern), not separate ST states — smaller diff, and the tree asset needed no rebuild (verified: 7/7 matched, zero missing-external-data). Repair claims live in a runtime TSet, never serialized.
- **Compile:** 2 iterations (missing forward decl of FRHDepositState in the visualizer header), then clean 30 s.
- **Incident, resolved:** the project `Content/` folder was found Finder-trashed mid-session (editor recreated a stub; headless run died with "package does not exist"). Restored intact from `/Volumes/Unreal/.Trashes/501/` — zero loss; git-diff clean against HEAD afterward. Rule adopted: **commit harness assets BEFORE the verification run** (BP_M1bTest went in as an insurance commit `1761519`).
- **Verification (BP_M1bTest headless, ~25 min + autosave tail run):**
  - Pre-survey `Dig Ice_A` → `ORDER REJECTED: Unknown deposit 'Ice_A'`; survey posted; scout drove out; `=== SURVEY: Ice_A discovered - 30 t Ice at (130, 130) m ===`; same dig order then designated. **The first go-look moment works.**
  - `RH.Wear 85`: all 7 robots `work rate x0.30 degraded` (exact linear math); RC-M triage `85 wear (4 parts)` + `50 wear (2 parts)` → **all 6 starter parts burned, triage stops**, five robots limp on; natural accrual visible meanwhile (85.0→88.9 while working).
  - Quota **Sol 6 ON TIME** (one sol later than baseline = the designed cost of surveying before drilling); PartsCrate manifest launched.
  - Tail via `RH.Load auto` (wear survives save/load, proven implicitly): `SHIP LANDED (Sol 12)` → PartsCrate → RC-M immediately resumed: 97+94+50 wear removed, **exactly 10 parts** — resupply loop closed.
- Remaining: remove the placed BP_M1bTest_GateB actor from L_Slice (next editor session), then Gate C — the player's eyes (inspection card, fleet panel, notice-channel unification, PAUSED banner).

## 2026-07-06 — Session 13 (M1-b Gate A: Z-model + placement honesty — verified headless)

- **Scope (per `docs/m1b-working-spec.md`, director-approved):** the `(X, Y, Level)` coordinate lands across the sim — `FRHCommand`, `FRHBuildingInstance`, `FRHDepositState`, `FRHRobotSaveState` carry `int32 Level`; `LocationCm.Z` is derived (`Level × FloorHeightCm`), never authored; every spatial query is per-level 2D (coverage, link range, deposit lookup/attach, all three task-board haul routings, era logistics, all four robot claim APIs — level params default to surface so agent-band callers are untouched until robots can descend in M1-d). Non-surface placement rejects with "Floor −N is not connected - no shaft". Save format **v2** (v1 refuses loudly). Config rows `FloorHeightMeters=4` / `MaxDepth=5` in CSV + live DT_Config (read-back verified; `config=31` in the run log). **Footprint/overlap validation** in `CanPlaceBuilding`: rectangle test on same-level instances, strict inequality so edge-adjacent stays legal — checked against the reel, hand-played, and harness layouts before compiling (tightest pair clears by <1 m). Deck gains the **shaft-strip stub** (SURF lit, −1…−5 dark from data) — the M1-d elevator panel's home.
- **Deviation from spec, logged honestly:** robots got Level only as a dormant save-state field; the Mass fragment/spawn path stays 2D until the shaft exists (M1-d) — the claim-API seam is where it will thread through.
- **Compile:** clean first try, 36.8 s cold, editor closed.
- **Verification 1 — full M0-c arc, headless (`BP_M0cTest` placed + `-game -nullrhi`, ~24 min):** **zero rejections** (the new footprint rule accepted every legal order); quota **Sol 5 ON TIME** (exact match to Session 8 + Gate B baselines); manifest 6 items / 5100 kg (exact); steady-state sol cadence exactly 150.0 s/sol; ship transit **exactly 3.00 sols**. One surface delta, root-caused: end card says ship landed **Sol 10** where the baselines said Sol 9 — the harness's manifest/launch beat is *wall-clock* scripted and fired at sol 7.23 (just past the boundary the baselines launched just before), because `StartSolHour=6` (Gate C-era dawn start, post-baseline) shifted the mission clock +0.25 sol and headless boot adds a load-hitch catch-up burst. Same beat, same 3-sol math, stale baseline — **not** sim divergence. Note for M1-c: script future harness beats in sim time, not wall time.
- **Verification 2 — save v2 round-trip, headless:** `RH.Load auto` on the parity run's sol-10 autosave → `Colony loaded: slot 'auto' (sol 10, 17 buildings, 7 robots, 2 tasks)`, 7/7 robots respawned and matched by the StateTree brain processor.
- Remaining before Gate B: harness removal from L_Slice (editor gate), director 30-s in-Play sanity check (save/load buttons, shaft strip visible, overlap ghost goes red on an occupied cell).

## 2026-07-06 — Session 12c (Gate C hand-played sign-off — M1-a CLOSED)

- **Reel driver retired** from L_Slice by commit `705137a` (the asset stays in `/Game/RedHope/Core` with the harnesses) so the director's run started from a quiet colony.
- **The director's first hand-played session, verified against the editor log:** dig designated on Regolith_A (60 t), then 2× SolarArray, BatteryBank, ChargePad, and the Forge — every order placed through the deck's click-to-place flow, every one crossed the 45 sim-s uplink and opened a construction site, **zero rejections**, all sites completed. Struct observed climbing (Forge batches) and dropping (construction consumption) simultaneously — the production/spending loop read correctly to the player. Sol autosaves fired throughout.
- **Director verdict: "controls are fine — functionally it's working."** Camera (WASD pan / wheel zoom / RMB-drag orbit / MMB-drag pan) and deck ship as the M1-a baseline. Director flagged gameplay + game UI as the axis to keep improving — folded into the M1-b plan (playability strand).
- **Gate C findings (queued M1-b, UI batch):** (1) the run stalled at the first step because the sim was paused and the only cue is the small `PAUSED` readout text — needs a prominent paused treatment; (2) placement/dig/confirmation prompts (`ShowToast` ghost/transmit messages) render under the help box exactly like the rejection toasts we already fixed — route ALL player-facing prompts through the deck's Slate notice channel and retire the GEngine debug path for gameplay feedback.
- **M1-a is closed:** Gates A (sim spine), B (StateTree brain), C (hands) all verified — the game is hand-playable end to end. Next: M1-b working spec (Z-model + fleet reality + inspection card), agent-proposed, awaiting director OK.

## 2026-07-06 — Session 12b (reel v3: the full quota→launch→landing arc, verified on footage)

- **Recording #4 (~17 min) diagnosis — reel v2 pacing bug, not a sim bug:** Pylon #2's order arrived 65 s before Pylon #1 completed (the Delay-150 estimate ignored robot workforce contention — the Forge's 400 fab-s hogged the crew — and blackout-frozen construction; pylon 1 actually completed ~1860 sim-s after its order). Rejected on link range → IceDrill/WaterPlant rejected outside coverage → no Water/Oxygen → Quota Q1 (needs Struct AND Water AND Oxygen) never met → `RH.Manifest`/`RH.Launch` silently no-op'd for 7 sols. Second find: the rejection toast **provably never rendered** (frame 0.7 s after the log line, blank screen) — `AddOnScreenDebugMessage` draws under the deck's opaque Slate help box. Third: `RH.Showcase` had been run manually in all three demo sessions, doubling idle load and overlapping the reel's Electrolyzer — demo runs are now explicitly hands-off.
- **Fix batch:** reel **v3** (pylon-2 beat 150→270 s, ice beat 90→150 s, manifest/launch windows 3→5, on-screen hands-off banner); **deck notice line** — the visualizer keeps the last rejection (`GetNoticeText`, 12 s real-time hold so it survives pause and 8×) and the deck polls it into an orange line under the colony readout: the visible rejection channel. Robot night-glow bump (0.6/0.5/0.28) went live in the same compile (21 s clean, cold).
- **Recording #5 (21.5 min, fresh Play, zero console commands — log confirms): the full M0 arc on camera for the first time, zero rejections.** Pylon chain held at the new beats; Water flowing by sol 4.5 (270/200 while Electrolyzer still building), Oxygen 89/50 by sol 5.5. `=== CEO TRANSMISSION (Sol 5): Quota Q1 met ON TIME ===` — same sol as the M0-c scripted run and the Gate B parity run, now emergent under the reel's timings. Green CEO toast + `QUOTA MET - manifest 0/10000 kg` readout line legible on camera; first manifest window composed 2300 kg and launched; `SUPPLY SHIP INBOUND` readout; `=== SHIP LANDED (Sol 8) ===` with the cyan landing toast, the ship visual, and the cargo build-credits appearing in the palette (SolarArray [2]→[3], BatteryBank [1]→[2]). Readout growing quota→manifest→inbound→continues lines end-to-end.
- **Observations:** once ISRU runs 24/7 the colony lives in permanent deficit (night load ~2005 W vs gen 50 W; SHED:9–10 every night; bank pinned near 0/6000) — quota timing unaffected since ISRU, not power, is the critical path; same brownout-throttling read as Session 9, still worth a UI treatment later. Robot night glow **unverified** — the static demo camera never catches the crew at night (they're at the ice field off-frame). Notice line **unexercised** this run (zero rejections — the good problem); stays as the safety net. Sim opens at sol 0 ~25% with 140 kg starter Struct — consistent across runs.

## 2026-07-06 — Session 12 (demo reel + recording-review fix batch — verified on director footage)

- **Demo pipeline root cause + fix:** recording #2 showed "no movement but the sun" because the `BP_DemoReel_Driver` had been placed in a deliberately-unsaved level — quitting the editor for the compile discarded it, so Play had no driver. Re-placed and **saved into L_Slice** (OFPA external actor; `save_actor` failed on the fresh package, `save_assets []` = Save All worked). Rule: demo drivers get saved, then removed by commit when retired.
- **Fix batch (compiled clean, verified against recording #3 — 163 s, one full sol):**
  - Construction sites: 4 hazard corner posts + mast work lamp — verified legible day AND night; the site→completed swap reads clearly in footage.
  - Era refusal now restores `LastAgentSpeed` + broadcasts a toast instead of silently dumping to 1× — compiled in, **still unexercised** (the one 60× press on record legitimately engaged era: nothing was under construction).
  - Robot night glow: 0.1 emissive proved invisible in footage → bumped to (0.6, 0.5, 0.28) this commit, **pending next compile + footage check**.
- **Reel v2 first act verified from the sim log:** 9 uplink orders in one burst, every one executed at exactly +45 sim-s; fabrication times per DataTable (Solar 60 s, ChargePad 120 s, Forge 400 s, Pylon 90 s); Forge batching 25 kg Struct per 100 sim-s; the Delay-150 pylon beat fired on time. Manifest/launch act not reached — director stopped at ~163 s; the full arc needs the ~12-min run.
- **Accidental stress test, passed:** `RH.Showcase` was run pre-recording (double idle load). Overnight the bank hit 0/10000 → **SHED:13** in the readout, construction froze, and dawn restored loads + resumed building with no errors — the load-shed system's first end-to-end showing in footage. The sol-1 autosave fired mid-recording (slot 'auto', 19 buildings, 7 robots).
- Notes for later: era→agent speed drop dumped 43 sim-s with a warning (accumulator flush; fold into M1-c era work); showcase IceDrill correctly warns "no deposit within 12 m"; SolarArray glass glow reads oddly bright at night (helps legibility; art-pass item).

## 2026-07-06 — Session 11 (Gate C visual rounds: designed gray-box — committed `ed1b71e`)

- **Director-driven iteration, screenshot-verified each round** (director played + screenshotted; agent diagnosed + fixed + compiled on editor-close gates — 6 compile cycles, all clean, ~13 s each once warm):
  1. **Upside-down/mirrored ground labels** — root cause: the label rotation was an Euler combo that faced the TextRender's front INTO the ground (camera read the back: mirrored + flipped). Diagnosed with two Simulate-mode captures (face fix vs in-plane flip are separate errors); final fix `MakeFromXZ(Up, +Forward)` — a pure rotation, never a reflection.
  2. **"No detail" round** — greeble: Forge chimney/slag-chute/catwalk, IceDrill 4-strut derrick + spoil auger, WaterPlant header pipe + valve wheels, BatteryBank rack rails + coolant fins, Electrolyzer manifold; SolarArray rebuilt as framed PV panel (mullions, A-frame mount, faint blue glass glow).
  3. **"No texture" round (material, MCP-only)** — `M_Graybox` gained metallic 0.6 + noise-varied roughness, then world-aligned **panel seams** (1.2 m grid, Frac→|d|→smoothstep, darkened into BaseColor), **per-panel value variation** (VNF_CellnoiseALU on the same grid), and a **dirt-skirt** Z-gradient at hull bases — all gated by a `SeamStrength` scalar so `MI_MarsGround` (SeamStrength=0, Metallic=0) keeps clean regolith. Deposit slabs keep the grid: reads as survey marking, kept deliberately.
  4. **"Bland and boxy" round (composition)** — foundation **plinths** under every structure; **50 cm saturated function-color band** per machine (emissive floor ×0.9 so hue survives midday exposure); **Lander rebuilt as a descent stage** (hull 60% of footprint on splayed legs + footpads, engine bell + throat ember, propellant spheres, capsule + dish — proportion, not decoration, is what killed the table-on-stilts read); **Electrolyzer rebuilt** as slim hall + two big domed flank tanks on cradles; **WaterPlant** block shrunk so the tank farm dominates; **Forge control annex** with lit crew windows; **ComputeModule** as dark server tower (status strips, cooling collar, uplink dish); BoneWhite darkened 0.62→0.52 for ground separation.
- **`RH.Showcase`** (`RHCheats` + `URHSimWorldSubsystem::Debug_Showcase`): one completed instance of every building type on a grid, bypassing the economy — the visual-QA harness for every round above, and for the underground camera work to come.
- **Toolchain lessons (memory updated):** (a) MCP viewport capture reads the editor world, NOT the PIE/Simulate world the director sees — showcase verification runs through the director's screenshots; (b) **in-memory material edits die with the editor session — `save_assets` after every MCP wiring batch** (one full node graph was lost to an editor restart and rebuilt); (c) external-drive disconnects mid-session reset the shell cwd and can zombie the build — verify volume + port 8000 before every compile; a build against an open editor emits a `-0001` hot-patch dylib that the next cold launch ignores — always relink clean after the editor closes.
- **Director ruling:** ship this as the Gate C look and move on; a deeper bespoke art/design pass on the buildings is **deferred to a later milestone** ("come back… when it makes more sense to improve our art design"). Logged in design-decisions.
- Committed `ed1b71e`. **Still open to close M1-a: the Gate C daylight hand-played sign-off** (fresh PIE, no showcase: dig → power spine → Forge → 8× → Struct climbing, camera/deck verdict), then the gate log.

## 2026-07-05/06 — Session 10 (M1-a Gate B: StateTree robot brain — verified, behavior-identical)

- **Architecture landed as specced (§5b):** the robot brain is now a StateTree asset (`/Game/RedHope/AI/ST_RobotBrain`) — Work + Charge states, `FRHNeedsChargeCondition` on the Work→Charge transition — with the tasks calling the exact sim API the M0-c switch called. `URHRobotBrainProcessor` ticks each robot's tree once per fixed sim sub-step (MassAI's plumbing, never its frame/signal scheduling: determinism preserved). Legacy switch processor stays live for robots without the ST fragment; `RH.BrainMode 0` forces it — two brains, one driver, permanent A/B instrument.
- **The tree asset is authored in code** (`RH.BuildRobotStateTree`, WITH_EDITOR): states, tasks, transitions, condition, compiled via `FStateTreeCompiler` — reproducible source, no GUI state. Toolchain trap #1: **assets created by console commands while Simulate runs inherit the PIE package flag and every save path silently skips them** — fixed with a duplicate → delete → rename-back launder through clean packages (no recompile needed).
- **The silent parity failure, and the 5.8 lesson that cost a day-part:** first ST run — robots inert, zero errors, zero logs. Diagnosis chain: probes showed the processor constructed + queries configured but `Execute` never called; the A/B run proved legacy fine; headless probes localized it. **Root cause (verified in engine source): the sync `CreateEntity(FragmentInstanceList, SharedValues)` derives its archetype with an EMPTY const-shared bitset and `InternalBuildEntity` never reconciles it** (only the async path upgrades via `GetOrCreateSuitableArchetype`). Our robots' archetype lacked the `FMassStateTreeSharedFragment` bit → the query's `AddConstSharedRequirement` matched zero archetypes → **Mass prunes processors with no matching archetypes, so Execute is never even called.** Fix: build the ST archetype composition explicitly (fragments + engine ActivatedTag + const-shared bit), spawn against it, set fragment data directly. Second engine trap defused en route: MassAI's activation processor would have **re-allocated our instance data and Started trees on wall-clock cadence** — robots pre-carry `FMassStateTreeActivatedTag` so the engine machinery never sees them; dynamic ST processors disabled by config.
- **New workflow discovered under pressure — the editor-free verification loop:** `UnrealEditor-Cmd <proj> <map> -game -nullrhi -stdout -FullStdOutLogOutput` runs the real game world headless (Mass phases, subsystems, fleet spawn all live). The archetype fix was verified this way — 7/7 robots matched, trees Running, Work ticking — before the editor ever reopened. Compile-and-verify without a director gate; this changes our iteration economics.
- **Parity verdict (BP_M0cTest re-run, StateTree brains, zero manual input):**
  - `=== CEO TRANSMISSION (Sol 5): Quota Q1 met ON TIME ===` — **same sol as the legacy run.**
  - `=== SHIP LANDED (Sol 9). Cargo transfer: 6 items, 5100 kg ===` — identical manifest, all cargo effects live, lag 45→8 s.
  - `=== THE PROGRAM CONTINUES: sols elapsed 9 | quota met sol 5 (deadline 10) ===` — **end card identical word-for-word.** Zero rejections, zero tree-start failures.
  - The matched-population signal ticked 7→**8** when the manifest's RC-E2 spawned — manifest-spawned robots join the StateTree brain mid-run.
  - Power numbers ran healthier than the M0-c original (bank 9054/10000 at sol 1 vs 0/10000) — that delta is the two director-approved Gate A balance fixes, not the brain; quota timing held regardless because the ISRU build-out, not power, is the critical path.
- Gate B closed. Next: **Gate C — hands** (camera/input wiring + Slate command deck v1; the first hand-playable build).

## 2026-07-05 — Session 9 (M1-a Gate A: save/load, era mode, CostResources, balance fixes — verified)

- **Compile:** 2 iterations (one missing forward declaration of `FRHRobotRow`); 21.5 s. One zombie-editor quit killed per the established procedure (hung in-editor shell held port 8000).
- **Headless proof (editor closed):** `UnrealEditor-Cmd -run=RHSim -sols=20` booted a bare Game world, brought the colony online (Lander, 7-robot fleet, dig designation through the 45 s uplink), era-integrated 20 sols in ~0.1 s CPU, printed the ledger, exited clean. The standing-order "headless + faster than real time" requirement is now demonstrated, not promised. Honest gap observed: with no production buildings, era logistics extracts nothing (it digs only to feed demanding consumers) where the agent band would stockpile raw regolith — quantified below, harness in M1-c.
- **DataTable path change:** delete+reimport failed this time ("already exists" after a true-returning delete) — but recompiled row structs already give live DTs the new columns, so `set_rows` wrote the new values directly (verified by read-back). Simpler than delete+import; adopted for column-addition cases.
- **Gate A SIE run (`BP_M1aTest`, ~10 min at 8×/60×, zero warnings or errors):**
  - **Save/load equivalence — exact.** Saved `siteA` at sol 0.98 mid-construction (stockpile site open, 1 task) mid-batch, ran +400 sim-s to a control checkpoint C1, loaded, re-ran the same span: C2 = `Sol 1 31% | gen 398 W load 835 W battery 0/6000 | Struct 170 | tasks 0` — **identical to C1 line for line.** The replay leg even re-fired the same sol-1 autosave.
  - **Balance fixes visible in the wild:** night status shows `gen 50 W` (Lander RTG trickle, was 0 in every M0 run); battery bank completion credits capacity AND the half-charge (344/6000 pre-sol-1).
  - **Era mode:** engaged at 60× — 0.4 sol integrated in 8 real seconds mid-run, then a full sol in 20 s on the divergence leg; autosaves fire at era sol boundaries; agent band resumes cleanly at 8× after. Auto-drop guard present but untriggered this run (construction had finished before each engage; the refusal path gets exercised in M1-c's storm auto-drop).
  - **Divergence sample (agent vs era, 1 sol from the same `clean` save):** Struct 570 (agent) vs 520 (era) — **8.8% divergence, above the proposed 5% bar.** Cause identified: era steps decrement batches in 1-min quanta with no overshoot carry, so a 100-sim-s Forge batch takes 120 era-sim-s (~17% slower cycling). Fix (batch overshoot carry) queued for the M1-c paired-run harness rather than patched blind mid-gate.
  - **CostResources:** every build this run parsed the new bill-of-materials column (all rows migrated `CostStruct_kg`→`CostResources`); the multi-line case arrives with M1-d's Habitat.
  - Emergent observation: a colony in sustained power deficit (bank 0, load 835 W vs ~400 W day gen) duty-cycles via shed oscillation — the Forge browns out to ~50% throughput instead of stalling. Reads as brownout throttling; acceptable behavior, worth a UI treatment later.
- `BP_M1aTest` kept as the Gate A regression harness (not placed). Gate B next: StateTree robot brain — needs the MassAI plugin enabled (director OK + restart, batched with the Gate B compile).

## 2026-07-05 — Session 8 (M0-c: the slice finale — charging, site delivery, shedding, ISRU, quota→ship)

- **Systems (committed `0f1fefc`, compiled clean):** robot charging etiquette (break off below 25% battery — unless mid-delivery — release the claim, dock at the nearest completed pad, draw the pad's 500 W from bank-then-live-surplus via `RequestChargeWh`, resume at 90%; both fractions are `DT_Config` rows; **no pad built = the robot works until it dies where it stands, deliberately** — that lesson belongs to the player). Construction-site material delivery (task board posts Struct hauls from any completed holder to the site; the fabricator stands by until the full load is delivered; the material is consumed into the structure at completion — replaces M0-b's instant colony-wide deduction). Priority load-shedding (bank empty + load > gen → shed lowest `LoadPriority` first until demand fits generation: Forge first, pads last; `SHED:n` in `RH.Status`). Extraction recipes (empty `Inputs` + `RequiresDeposit` → the batch draws its output mass from the deposit the building auto-attached to at placement; `DrillIce` row added). Pylon link-range rule (defs with coverage+link place by ≤80 m link to the nearest *completed* node; everything else still needs coverage). Quota state machine (Open → CEO transmission with on-time/late award by met-sol → manifest composition against the mass budget → launch → 3-sol transit → arrival applies cargo effects → end card), `RH.Manifest`/`RH.Launch` commands, quota toast + gray-box supply ship in the visualizer.
- **Smoke test (SIE, one scripted ~38-min run at 8×, `BP_M0cTest`, authentic 45 s uplink lag — no test shortcut): the entire M0 loop ran end-to-end with zero order rejections and zero manual interventions.**
  - Sol 0–1: pad, 6 solar, 2 banks, stockpile, Forge built from the 400 kg starter Struct via site deliveries; dig designated on Regolith_A.
  - Sol 1.5 / 2.7: pylons at (55,55) and (110,110), each accepted on link range to the previously *completed* node.
  - Sol 4–5.3: IceDrill on Ice_A (auto-attached), WaterPlant beside it in pylon coverage, Electrolyzer at base. The drill ran `→Ice:40` extraction batches immediately; the one-rule output router sent drill ice to the plant (`Ice:50→Water:45`) and fluids to the network pool — the full ISRU chain came alive with no special-case code.
  - **Sol 5: `=== CEO TRANSMISSION (Sol 5): Quota Q1 met ON TIME ===`** (deadline 10; 10,000 kg budget). Forge held ~170 kg Struct/sol; fluids filled within ~a sol of the chain going live (final: Struct 915/300, Water 1550/200, O₂ 454/50).
  - Sol 7: scripted manifest — SolarPack, BatteryPack, PartsCrate, Toolkit, ComputeCore, AdvExcavator = 5,100/10,000 kg — `Ship launched: 6 items, arrival in 3 sols`.
  - **Sol 9 (99%): `=== SHIP LANDED. Cargo transfer: 6 items, 5100 kg ===`** — every cargo effect applied live: import stock +1 solar +1 battery (visible in the next status), +10 SpareParts, fabricator speed ×1.15, `Compute core online: order lag now 8 sim-s` (45→8), and the RC-E2 spawned into the Mass fleet. **`=== THE PROGRAM CONTINUES: sols elapsed 9 | quota met sol 5 (deadline 10) | fleet + colony operational ===`** Post-launch manifest attempts all bounced with "No manifest is open" — phase enforcement demonstrated by the script's own redundant attempts.
- **Shedding evidence:** midnight statuses read `gen 0 W load 0 W battery 0/10000 Wh SHED:13..17` once the colony grew — at night on an empty bank the entire grid sheds, pads included: docked robots wait for sunrise (`RequestChargeWh` returns 0). Reads as intended drama; whether the Lander should hold a pad reserve is an M1 balance question.
- **Charging-etiquette evidence:** an RC-E runs ~0.75 sol on a full battery; the fleet stayed operational 14+ sols with fresh dig claims every sol — roughly a dozen seek→dock→resume cycles per excavator, including shed-night waits docked at the pad.
- Captures: `docs/media/m0c-colony-sol4.png` (night, chain going up), `m0c-ship-landed.png` (dawn, supply ship on the pad), `m0c-isru-chain.png` (drill + plant on the ice, pylon line back to base).
- **End-of-run benchmark** (`RH.Benchmark m0c_end`, sol 14, 17 buildings + 8 task-driven robots + full production/power sim at 8×, in-editor SIE): avg 41.19 ms (24 fps), p95 42.80 ms, worst 48.95 ms, mem 6,420 MB — at or under the empty-world scaffold baseline (46 ms). The complete slice sim is unmeasurable against the editor render floor; the M2–M4 population headroom conclusion stands.
- **Known issues logged, deliberately not fixed mid-verification:** (1) battery packs' arrive-half-charged credit is clamped away while the bank is under construction (capacity only counts completed storage) — move the credit to construction completion (M1); (2) `RH_Buildings` notes say the Lander "trickle gens" but generation multiplies by the solar curve → 0 at night; reconcile note vs sim in the M1 balance pass; (3) manifest cargo effects are name-keyed in code at slice scale — data-driven effect verbs are the logged M1 task; (4) hand-placed `GB_Dep_*` markers in L_Slice now duplicate the sim-spawned deposit visuals — cleanup queued.
- **M0 exit: all three stages verified in-engine (a: power/territory · b: production spine · c: finale).** The vertical-slice loop — dig → smelt → extend the grid → ISRU → quota → manifest → ship — is complete and self-sustaining. `BP_M0cTest` kept as the regression harness (not placed in the level). Stopped for director review.

## 2026-07-05 — Session 7 (M0-b: production spine — dig/haul/build/smelt, quota)

- **Systems (compiled in 2 passes; 1 header omission fixed):** sim world is now the single sub-step driver (uplink → task board → production → power). Deposits load from `DT_Deposits`; `RH.Dig` designates through the uplink. Task board posts haul work by recipe demand and build work from timed construction. Robots are task-driven Mass agents (`FRHRobotFragment`/`FRHTaskFragment`, claim→travel→work→deliver in `URHRobotTaskProcessor`); starting fleet spawns from `DT_Robots.StartingCount` on the first sim step. Struct costs deduct colony-wide; import-only hardware consumes starter flat-pack stock. Production runs recipes on the hybrid logistics split (solids in hoppers, fluids to the network pool); batches stall on power deficit; active-vs-idle draw feeds the ledger. Quota progress vs Q1 in `RH.Status`.
- **Smoke test 1 (unpowered, scripted BP_M0bTest):** the full chain ran — fleet ×7, designation, dig at exactly 2×50 kg/h (128 kg per 64 sim-s measured), haulers skimming piles to the Lander, Forge cost 250 kg (400→150), fabricator worked the 400 s site down, hopper fed, batch started — **then the colony hit `gen 47 W load 820 W battery 0 DEFICIT` and the batch froze at 0.82 h remaining.** The test built no solar; the deficit-stalls-production rule fired exactly as specced. "Every watt matters" emerged un-scripted in the first run.
- **Smoke test 2 (powered, BP_M0bTest2):** 2 solar + battery bank + Forge through the uplink; fabricator built all four sites serially; battery cap 2000→6000 Wh; sunrise charging (1879→4788 Wh); Forge fed and **self-sustaining: 4+ batches completed and auto-restarted (Regolith:100 → Struct:25 each), quota Struct 150→225/300 kg and climbing**, with the 825 W load bridged by the bank against the falling afternoon curve — no deficit.
- Capture: `docs/media/m0b-colony-running.png` (soft — TAA never converges at 8× sim speed; flagged for the look pass). BP_M0bTest/BP_M0bTest2 kept as regression harnesses.
- Known M0-c scope: robot charging etiquette (drained robots currently halt for good), material delivery to construction sites, load-shedding by priority, ISRU chain smoke test (drill/plant/electrolyzer), quota completion → manifest → ship arrival.

## 2026-07-05 — Session 6 (Mars sky pass + M0-a: definitions, power, territory)

- **Mars sky (director directive, rover-referenced):** VolumetricCloud + template SkySphere deleted (no clouds on Mars). SkyAtmosphere inverted from Earth: thin warm Rayleigh (butterscotch dust dome), cool high-anisotropy Mie lobe (blue sunset aureole per rover photos), reddish ground albedo, warm dust fog, 0.357° solar disk at 7 lux. Captures: `docs/media/mars-sky-midday.png`, `mars-sky-sunset.png`. Logged as the habitability-0 endpoints for the dial curves.
- **M0-a systems (compiled clean, 22.5 s, first try):** `URHDefinitionsSubsystem` (DT/CT registry; sole owner of `CanFabricateLocally`); sim world rewritten as tickable colony owner — data-driven buildings, power ledger (gen × `CT_SolarDiurnal`, idle loads, battery Wh clamp, deficit flag), coverage-disc territory with Build rejection, uplink queue pumped at sub-step boundaries; clock refactored to published per-frame step counts (multi-reader timeline; single-driver refactor queued for M0-b); colony visualizer (gray boxes from sim events + coverage disc debug draw + on-screen rejection toasts); console commands `RH.Build` / `RH.Status` / `RH.Lag`. Hand-placed gray-box colony + StressDriver removed from L_Slice (sim spawns the colony; BP kept as benchmark harness).
- **M0-a smoke test (SIE, scripted via BP_M0aTest — kept as regression harness):** all six checks passed in one run:
  1. Bootstrap: Lander #1 at (0,0); lag 45 sim-s from DT_Config; sol length 1200.
  2. Night power: gen 0 W at sol 0–17%, battery draining at exactly load × sol-hour rate (23 Wh over 48 sim-s at ~25 W — units doctrine verified).
  3. Uplink: Build Pylon queued visibly ("executes at t=56, now 27"), landed after 45 sim-s.
  4. Territory chaining: SolarArray at (50,0) accepted only because the new pylon's 40 m disc covers it.
  5. Territory rejection: Stockpile at (200,0) traveled the 45 s uplink and was rejected on arrival — "Outside grid coverage - extend pylons first."
  6. Sunrise: at sol 37%, gen 278 W (curve × panel + lander trickle), battery charging 889→1256 Wh.
- Next (M0-b): single sim driver ordering, Struct costs + build time, dig/haul/production loops, robots as task-driven agents, deposit visuals from data, quota tracking.

## 2026-07-05 — Session 5 (Step 4 scaffold, part 2: content, benchmark, smoke test)

- **Content built via MCP:** 8 DataTables imported from `docs/data/RH_*.csv` against the compiled `FRH*Row` structs (row values spot-verified; `ImportOnly`/`UnlockTech` live). `CT_SolarDiurnal` (diurnal curve), `CT_AtmosphereDial` (FogDensity, SunIntensityMul), `CT_CameraRig` (Distance/Pitch/FOV) populated. `MPC_Atmosphere` (Habitability/TimeOfSol/DustAmount/SkyTint) + `M_MarsDial` MPC-driven material. Sun tagged `RH.Sun`.
- **Gray-box L_Slice:** Mars ground plane (5×5 km) + lander, 3 solar arrays, battery, pylon, charge pad, Forge, stockpile, 6 deposit markers at `RH_Deposits` coordinates. **Gotcha:** duplicating the engine OpenWorld template does not bring its World Partition landscape proxies (external actors stay under `/Engine`); the orphaned Landscape actor was removed and a plane used instead — real terrain is an M0 map-pass task.
- **Console-exec gap workaround:** MCP has no console-command tool, so `BP_StressDriver` (authored via the Blueprint graph DSL) fires the benchmark sequence from BeginPlay: baseline → 200 agents → 8× → 500 agents → 8×, via `RH.SpawnDummies` / `RH.SetSpeed` / `RH.Benchmark`.
- **Hardware benchmark (director-required) — PIE Simulate, in-editor viewport, Lumen defaults, this Mac (arm64):**

  | Phase | avg ms | fps | p95 ms | worst ms | memory |
  |---|---|---|---|---|---|
  | 0 agents, 1× (baseline) | 46.29 | 22 | 50.73 | 58.08 | 6,214 MB |
  | 200 agents, 1× | 46.73 | 21 | 51.51 | 56.95 | 6,217 MB |
  | 200 agents, 8× | 46.91 | 21 | 51.44 | 58.75 | 6,222 MB |
  | 500 agents, 1× | 47.77 | 21 | 52.47 | 202.82 | 6,233 MB |
  | 500 agents, 8× | 47.91 | 21 | 88.67 | 190.05 | 7,342 MB |

  **Reading:** agent cost is in the noise — +1.6 ms at 500 agents × 8× sub-stepping (≈4,000 agent-updates/frame-equivalent) over an empty world. The 46 ms floor is the in-editor Lumen viewport on this hardware, not the sim. Memory +19 MB for 500 agents+instances (the 1.1 GB jump in the last phase coincides with the HighResShot capture, not agents). The p95/worst spikes at 500 appear at capture/GC moments. **Conclusion: population targets for M2–M4 (hundreds of agents) are not hardware-limited on this machine; the budget pressure is rendering quality settings, which are tunable.** Numbers are in-editor Simulate — packaged builds will run faster; deltas are the architecture signal.
- **Smoke test:** PIE-Simulate ran the full 95 s sequence unattended; sim clock speed tiers exercised (1×/8×); Mass wander processor + battery drain sub-stepping live; ISM visualizer tracked 200→500 entities; atmosphere subsystem wrote MPC + drove the tagged sun. Captures: `docs/media/scaffold-graybox-editor.png` (first layout), `scaffold-graybox-grounded.png` (grounded pass), `scaffold-sie-500-agents.png` (live Simulate, 500 agents).
- Editor sky is still Earth-blue (template sky rig); Mars-ifying the sky/fog via the dial curves is the M0 look pass, deliberately not scaffold scope.

## 2026-07-05 — Session 4 (Step 4 scaffold, part 1: repo, sources, template removal, first compile)

- `git init` (branch main); baseline commit `557d902` = pristine pre-scaffold restore point (director requirement: lands before any deletion).
- Wrote Source tree: `RedHopeSim` (sim clock w/ fixed 0.1 s sub-steps + speed tiers, sim-world subsystem w/ stocks ledger + uplink latency queue, Mass fragments + dummy-agent spawner, wander processor with per-sub-step integration, DataTable row structs incl. `ImportOnly`+`UnlockTech` per directive, commandlet stub) and `RedHope` (GameMode/GameState/PC, strategy pawn w/ exponential zoom mapping, atmosphere subsystem [MPC writer + `RH.Sun`-tagged sun driver + `RH.Habitability` scrub CVar], ISM agent visualizer, `RH.SpawnDummies`/`RH.SetSpeed`/`RH.Benchmark` console commands).
- All Mass API usage verified against the 5.8 engine headers before compiling. 5.8 restructured Mass: **MassCore is engine-core** (`Runtime/Mass/MassCore`, owns `FTransformFragment` + `FMassEntityHandle`); `ConfigureQueries` now takes `TSharedRef<FMassEntityManager>`; `ForEachEntityChunk` dropped its EntityManager arg; the **processing-phase ticker (`MassSimulationSubsystem`) is in the MassGameplay plugin** — hence MassGameplay is required for agents to tick at all.
- Director-approved deletions executed with `L_Slice` open (never the open level): `Content/TopDown`, `Characters`, `LevelPrototyping`, `Cursor`, and their `__ExternalActors__`/`__ExternalObjects__` subfolders. `L_Slice` created by duplicating the engine OpenWorld template (World Partition) — MCP has no new-level tool. Config retargeted (startup/default map → `L_Slice`, GlobalDefaultGameMode → `/Script/RedHope.RHGameMode`).
- `.uproject`: added Modules (RedHope, RedHopeSim), enabled MassGameplay (director-approved, one restart).
- Session moved from the in-editor Terminal plugin to an external terminal for the build (editor quit kills in-editor sessions; `.mcp.json` → `http://127.0.0.1:8000/mcp` reconnects externally).
- **Compile: success** (red_hopeEditor, Development, arm64; 32 s; 2 iterations). Gotcha for the record: `FMassEntityHandle` needed an explicit `#include "Mass/EntityHandle.h"` — not reliably visible via `MassEntityTypes.h` in a fresh TU. `ExecutionFlags` is `uint8` in 5.8.
- Next (pending director reopening the editor): CSV→DataTable imports, MPC_Atmosphere + curves + sky rig, gray-box slice map, 200/500-agent benchmark @1×/8× (frame ms + memory, logged here), PIE-Simulate smoke test + captures.

## 2026-07-04 — Session 3 (Step 3: architecture + toolchain stress tests)

- Logged Step 2 approval + import-only-is-removable directive in design-decisions.md.
- Stress-tested MCP toolchain in-editor (all artifacts under /Game/RedHope_StressTest, deleted after):
  - MPC pipeline verified end-to-end: created MPC_DialTest, wrote scalar+vector params via ObjectTools (auto-GUIDs confirmed via round-trip read), created M_DialTest, added CollectionParameter expression, bound to collection + wired to BaseColor, recompiled clean. **The atmosphere-dial backbone is fully MCP-buildable.**
  - CurveTable created + solar diurnal keys set/verified. CSV import path exists.
  - DataTable create works but requires existing C++ FTableRowBase structs (no struct-creation tool; registry shows engine structs only) → workflow: agent writes C++ structs → director compiles → agent imports CSVs.
  - Blueprint create (arbitrary parent) + compile verified; graph DSL available for BP logic.
  - BlueprintTools describe overflowed context limits; worked from saved-file extraction.
- Engine facts verified: MassEntity is engine-core in 5.8 (Engine/Source/Runtime/MassEntity); MassGameplay + MassAI plugins present but not enabled; GameplayStateTree already enabled in uproject.
- Delivered `docs/ue-architecture-proposal.md`. Stopped for director review. Editor left clean (stress-test folder deleted).

## 2026-07-04 — Session 2 (Step 2: M0 spec)

- Logged director approvals in `docs/design-decisions.md`: all eight §9 recommendations, map model (formally the ninth question), hybrid logistics, Hope as derived index, silent identity-axis accumulation.
- Delivered M0 vertical-slice spec: `docs/m0-vertical-slice-spec.md` — units/clock doctrine, two-timescale model (agent band 1×/3×/8× + specced 60× era-mode aggregate integrator), expansion-headroom decision (World Partition + authored sectors through v1), power/territory model, 5 robot types, Forge and ISRU chains, quota Q1, manifest catalog, slice map, confidence flags.
- Authored 9 DataTable-ready CSVs under `docs/data/` (config, resources, buildings, recipes, robots, manifest items, quotas, deposits, solar diurnal curve). Dormant M1+ rows included so later milestones activate content, not schema.
- Stopped for director review. No editor/project assets touched this session.

## 2026-07-04 — Session 1 (kickoff)

- Verified Unreal MCP toolchain: 20 toolsets enumerated and reachable. Engine confirmed UE 5.8.0 (Release-5.8, CL 55116800). Editor live with `/Game/TopDown/Lvl_TopDown` open.
- Round-trip test: spawned a StaticMeshActor `MCP_Handshake_DeleteMe` into the level via MCP, then deleted it. Level not saved; no persistent change.
- Design brief was **not present in the project tree**; located at `~/Downloads/files/the-red-hope-design-brief.md` and copied to `docs/the-red-hope-design-brief.md`.
- Seeded `docs/design-decisions.md` and this build log.
- Delivered Step 1 (digest, gaps, open-question recommendations) — archived at `docs/step1-digest-and-recommendations.md`. Stopped for director review per working rules.
- Noted: project is not a git repository. Recommended `git init` before any scaffold work (Step 4).
- No project assets or settings were modified this session.
