# Build Log — The Red Hope

## 2026-07-07 — Session 18 (M1-c Gate A VERIFIED in-engine: the storm/flare arc end-to-end)

- **DT_Events created via MCP** (FRHEventRow schema; canon rows Storm_1 sols 12–15 ×0.3 + Flare_1 sol 17 ×3, plus TEST rows Storm_T sol 2 / Flare_T sol 4 so verification and hand-play reach weather in minutes — TEST rows removed at M1-c close). `events=4` confirmed at boot.
- **BP_M1cTest headless run (~6.5 min at 8×), all pass:**
  - `ERA REFUSED: DustStorm imminent - holding 8x` then `ERA REFUSED: SolarFlare imminent` — both auto-drops fired at exactly the onset horizon, restoring the agent speed (never dumping to 1×).
  - Mid-storm status: `EVENT: DustStorm until sol 3.0 (severity 0.30; dust factor now 0.30)`, midday gen 309 W vs 1199 W clear-sky midday two sols later — the collapse is the data row. Colony rode the bank (6000 → 2838 Wh overnight) and came out clean.
  - Flare: `EVENT: SolarFlare (severity 3.00; dust factor 1.00)` — flares don't dim, they irradiate; working robots' wear visibly accelerated through the flare window (RC-E ~+3.0 wear in 0.3 sol vs ~+1 baseline), idle robots untouched. The tax reads exactly as designed: exertion under an angry sky costs triple.
- Harness retired from L_Slice by commit (asset stays in Core). Director hand-play of the M1-c surfaces is the remaining stage gate: storm banner + darkened sun at sol 2, uplink-panel order cancel, sparklines through the storm.

## 2026-07-07 — Session 17 (M1-c Gate A verified by paired-run iteration; Gate B flare tax + Gate C UI written)

- **The paired-run harness earned its keep — four era-leg iterations, each fix a physical constraint, never a tuning knob** (same sol-3 save; agent 8× vs era 60× to sol 9; `RH.Ledger` diff):
  1. Baseline: era **under**-extracted 8.4% and held 58% less raw regolith — the Session 9 "digs only to feed demanding consumers" limitation, measured. Fix: leftover era dig budget flows to stores (the agent band's pile→hauler→stockpile loop, integrated).
  2. Then era **over**-dug (deposit −10.7%): it ran 24/7 while agent robots halt after dark on an empty bank. Fix: the **night gate** — no sun + no stored energy = no work.
  3. Then still ~5% hot: daytime charge trips (25%→90% docked hours) weren't modeled. Fix: **derived duty factor** — work-hours vs charge-hours per cycle, every term a data row (RC-E ≈ 0.81). Not a magic constant.
  4. **Final: extraction 0.4%, stores 4.0%, battery ~0 — bar met.** `Struct +7.4%` is the one line over: root-caused to **brownout-throttle oscillation** — the power-starved test colony's Forge duty-cycles at sub-step granularity under shed oscillation (Session 9's observation), which a 1-min integrator cannot see; era's cleaner cycling over-produces. On a healthy grid this term collapses. Logged as a measured, understood deviation; harness (staged saves + RH.Ledger) stays the regression net. Known deviations by design: SpareParts (era models no RC-M repairs), robot wear static in era band.
- **Era-honesty fixes shipped en route** (commit `e84d3b3` + this batch): StepProduction time-budget integrator (overshoot carries across batch boundaries — kills whole-step rounding at era dt); clock accumulator debt carries across frames (the 43 sim-s era→agent dump, gone; only >120 sim-s backlog drops, loudly); `RH.Ledger` instrument.
- **Events live:** `FRHEventRow`/DT_Events path + RH_Events.csv (Storm_1 sols 12–15 solar×0.3; Flare_1 sol 17 wear×3); StepPower applies the storm dust factor; era refuses during events and within one era step of onset; RH.Status prints the active event.
- **Gate B written:** flare wear tax in AccrueWear (active flare multiplies exertion wear; docked-at-pad is NOT shelter until the M1-d vault — deliberate). Radiation config rows staged in CSV. Shielding build tax deferred to next session (changes CSVs + harness together).
- **Gate C written + compiled:** event banner (amber storm / red flare, top center); **cancellable uplink queue panel** (orders in flight with live Δ countdowns, × aborts before execution — M0 spec §8 delivered; the signal can beat the click, which is the game); **power strip-chart** (last 3 sols gen/load/bank as block-glyph sparklines in the readout); ComputeModule completion hook (lag 45→20 s, min() so ComputeCore tier 2 never regresses); storm sky presentation (Dust MPC scalar + sun intensity scaling with generation).
- Awaiting next editor session: DT_Events asset creation (struct now compiled), BP_M1cTest storm harness, director hand-play of the M1-c surfaces.

## 2026-07-07 — Session 16 (M1-b CLOSED by director hand-play; survey map; M1-c Gate A code)

- **M1-b closed on the director's hand-played verdict** ("everything feels pretty good… we are getting somewhere"): survey → Ice_A discovery, inspection card, fleet panel, PAUSED banner all exercised by hand. M1-c approved as specced.
- **Surveyed-land map (director request, same session):** sim-owned survey history (point/radius/sol/found; **save v4**); deck `Map` toggle → in-world coverage circles (teal = found, gray = empty) + Known Ground panel (every discovered deposit: type/tonnage/coords/dig status); empty surveys announce on the notice line. Verified headless (one hit + one empty survey, both recorded); committed `6e11507`.
- **M1-c Gate A written + compiled clean (verification in progress):**
  - `FRHEventRow` + `DT_Events` path (absent table = clear skies, graceful); `RH_Events.csv` authored (Storm_1: sols 12–15 solar×0.3; Flare_1: sol 17 half-sol ×3 wear — flare effects are Gate B).
  - **DustFactor finally moves:** `StepPower` multiplies solar by the active storm's Severity (via `GetDustFactorNow`); `RH.Status` prints the active event.
  - **Era auto-drop:** `CanEnterEraMode` refuses during any active event and within one era step of onset ("DustStorm imminent").
  - **Era-honesty fix 1 (the 8.8% divergence):** `StepProduction` rewritten as a time-budget integrator — batch progress, completion, and next-batch start all inside one step with overshoot carried, so a 2 h batch no longer costs whole-step rounding at era dt. Same function both bands (dimensional honesty preserved); agent-band change is sub-step-scale only.
  - **Era-honesty fix 2 (the 43 sim-s dump):** the clock's beyond-cap accumulator debt now CARRIES across frames (fast-forward at the per-frame cap) instead of being dropped on era→agent transitions and load hitches; only a runaway backlog past 120 sim-s drops, loudly.
  - `RH.Ledger` instrument: parseable per-stock lines (solids/fluids/deposits) — the paired-run harness's diff format.
- **Paired-run divergence harness running headless** (same sol-3 save → agent 8× vs era 60× legs → ledger diff; ≤5% bar): results next entry. Known deviation: 6-sol window pre-quota (era refuses near an unmet deadline — by design); the full 10-sol post-quota harness lands with BP_M1cTest next editor session, along with the DT_Events asset (row struct compiled this build).

## 2026-07-07 — Session 15 (M1-b Gate C: the player's eyes — compiled, awaiting hand-play)

- **All presentation, zero sim changes** (per spec §5 + the Gate C findings from the director's first hand-played run):
  - **Inspection card** (the panel promised to the director): click any building → card on the right column — name/id/position; construction state with per-material delivered/outstanding lines ("material Struct: 80 / 250 kg (hauling)"); ONLINE/SHED; live batch line with recipe + hours left; def power profile (gen/draw/idle/store); attached deposit with tonnage; hopper in/out contents. Cyan frame drawn on the selected building; click ground / right-click / Esc dismisses; selection survives ticks but clears if the building vanishes (load).
  - **Fleet panel** (right column, always on): per-robot line — def, state (idle/DIG/HAUL/BUILD/CHARGE/SURVEY/REPAIR), battery %, wear with degraded/HALTED/stranded flags; header shows fleet count + SpareParts stock. The wear system's legibility surface.
  - **Notice-channel unification:** `ShowToast`/GEngine debug retired from gameplay entirely. Three deck channels now: orange notice (rejections + discoveries, visualizer), cyan **confirm** line (order transmitted / save / load, PC-owned, 6 s real-time hold), and the **mode hint** — the placement/dig/survey prompt moved from invisible top-left debug text to a bottom-center bar above the palette, colored by validity.
  - **PAUSED banner:** bottom-center, dark red, "— PAUSED — Space or a speed key to resume" whenever speed is 0 — the Gate C session-start stall can't recur.
  - **Survey button** (ORDER group): click → cursor carries the scout's true reveal circle (radius read from the Scout row's WorkRate — preview honesty, same number CompleteSurvey uses) → click transmits the uplink Survey order. The discovery mechanic is now hand-playable, not console-only.
- **Compile:** clean first try, 55 s. Headless smoke run: deck constructs under nullrhi, sim boots, zero ensures/fatals.
- Remaining to close M1-b: remove BP_M1bTest_GateB from L_Slice (next editor session), then the stage-end **hand-played director run** — survey the NE ridge by hand, watch Ice_A appear, click buildings mid-run, read the fleet panel while robots wear.

## 2026-07-06/07 — Session 14 (M1-b Gate B: fleet reality — verified headless)

- **Scope landed (per spec §4 + charge etiquette):** wear accrues with exertion (any sub-step above idle draw, `WearPerActiveSol` from DT_Robots, clamped at halt); work rate degrades linearly 50→100 (`GetWearWorkMul`); at 100 the brain processor's halt gate stops ticking the robot entirely — repairable, never destroyed. RC-M **Repair**: claims the most-worn robot ≥ degrade threshold (parts in stock, self excluded, one claim per target), re-tracks a moving patient, spends SpareParts at 25 wear/part instantly on arrival. RC-S **Survey**: `RH.Survey <Xm> <Ym>` crosses the uplink → board task → scout drives out → deposits within `WorkRate` (60 m) reveal, marker + notice on discovery. **Discovery honesty end-to-end:** `SurfaceVisible=FALSE` rows (Ice_A/Ore_B/Ice_B — flags waiting in the CSV since M0) are invisible to FindDepositNear, dig designation ("Unknown deposit", no information leak), drill auto-attach, and the visualizer. **Charge-pad queue:** one umbilical — only the queue head draws; slot taken at docking so a robot that dies en route can't block the pad; legacy brain bypasses (frozen M0-c baseline). Save v3 (discovery + survey targets; wear already rode v1). Era mode refuses during field ops. New instruments: `RH.Survey`, `RH.Wear <0-100>`, `RH.Fleet`. StarterSpareParts=6 finally stocked at boot.
- **Deviation from spec, logged:** survey/repair are claim branches inside the StateTree's Work task (the dig/haul/build pattern), not separate ST states — smaller diff, and the tree asset needed no rebuild (verified: 7/7 matched, zero missing-external-data). Repair claims live in a runtime TSet, never serialized.
- **Compile:** 2 iterations (missing forward decl of FRHDepositState in the visualizer header), then clean 30 s.
- **Incident, resolved:** the project `Content/` folder was found Finder-trashed mid-session (editor recreated a stub; headless run died with "package does not exist"). Restored intact from `/Volumes/Unreal/.Trashes/501/` — zero loss; git-diff clean against HEAD afterward. Rule adopted: **commit harness assets BEFORE the verification run** (BP_M1bTest went in as an insurance commit `1761519`).
- **Verification (BP_M1bTest headless, ~25 min + autosave tail run):**
  - Pre-survey `Dig Ice_A` → `ORDER REJECTED: Unknown deposit 'Ice_A'`; survey posted; scout drove out; `=== SURVEY: Ice_A discovered - 30 t Ice at (130, 130) m ===`; same dig order then designated. **The first go-look moment works.**
  - `RH.Wear 85`: all 7 robots `work rate x0.30 degraded` (exact linear math); RC-M triage `85 wear (4 parts)` + `50 wear (2 parts)` → **all 6 starter parts burned, triage stops**, five robots limp on; natural accrual visible meanwhile (85.0→88.9 while working).
  - Quota **Sol 6 ON TIME** (one sol later than baseline = the designed cost of surveying before drilling); PartsCrate manifest launched.
  - Tail via `RH.Load auto` (wear survives save/load, proven implicitly): `SHIP LANDED (Sol 12)` → PartsCrate → RC-M immediately resumed: 97+94+50 wear removed, **exactly 10 parts** — resupply loop closed.
- Remaining: remove the placed BP_M1bTest_GateB actor from L_Slice (next editor session), then Gate C — the player's eyes (inspection card, fleet panel, notice-channel unification, PAUSED banner).

## 2026-07-06 — Session 13 (M1-b Gate A: Z-model + placement honesty — verified headless)

- **Scope (per `docs/m1b-working-spec.md`, director-approved):** the `(X, Y, Level)` coordinate lands across the sim — `FRHCommand`, `FRHBuildingInstance`, `FRHDepositState`, `FRHRobotSaveState` carry `int32 Level`; `LocationCm.Z` is derived (`Level × FloorHeightCm`), never authored; every spatial query is per-level 2D (coverage, link range, deposit lookup/attach, all three task-board haul routings, era logistics, all four robot claim APIs — level params default to surface so agent-band callers are untouched until robots can descend in M1-d). Non-surface placement rejects with "Floor −N is not connected - no shaft". Save format **v2** (v1 refuses loudly). Config rows `FloorHeightMeters=4` / `MaxDepth=5` in CSV + live DT_Config (read-back verified; `config=31` in the run log). **Footprint/overlap validation** in `CanPlaceBuilding`: rectangle test on same-level instances, strict inequality so edge-adjacent stays legal — checked against the reel, hand-played, and harness layouts before compiling (tightest pair clears by <1 m). Deck gains the **shaft-strip stub** (SURF lit, −1…−5 dark from data) — the M1-d elevator panel's home.
- **Deviation from spec, logged honestly:** robots got Level only as a dormant save-state field; the Mass fragment/spawn path stays 2D until the shaft exists (M1-d) — the claim-API seam is where it will thread through.
- **Compile:** clean first try, 36.8 s cold, editor closed.
- **Verification 1 — full M0-c arc, headless (`BP_M0cTest` placed + `-game -nullrhi`, ~24 min):** **zero rejections** (the new footprint rule accepted every legal order); quota **Sol 5 ON TIME** (exact match to Session 8 + Gate B baselines); manifest 6 items / 5100 kg (exact); steady-state sol cadence exactly 150.0 s/sol; ship transit **exactly 3.00 sols**. One surface delta, root-caused: end card says ship landed **Sol 10** where the baselines said Sol 9 — the harness's manifest/launch beat is *wall-clock* scripted and fired at sol 7.23 (just past the boundary the baselines launched just before), because `StartSolHour=6` (Gate C-era dawn start, post-baseline) shifted the mission clock +0.25 sol and headless boot adds a load-hitch catch-up burst. Same beat, same 3-sol math, stale baseline — **not** sim divergence. Note for M1-c: script future harness beats in sim time, not wall time.
- **Verification 2 — save v2 round-trip, headless:** `RH.Load auto` on the parity run's sol-10 autosave → `Colony loaded: slot 'auto' (sol 10, 17 buildings, 7 robots, 2 tasks)`, 7/7 robots respawned and matched by the StateTree brain processor.
- Remaining before Gate B: harness removal from L_Slice (editor gate), director 30-s in-Play sanity check (save/load buttons, shaft strip visible, overlap ghost goes red on an occupied cell).

## 2026-07-06 — Session 12c (Gate C hand-played sign-off — M1-a CLOSED)

- **Reel driver retired** from L_Slice by commit `705137a` (the asset stays in `/Game/RedHope/Core` with the harnesses) so the director's run started from a quiet colony.
- **The director's first hand-played session, verified against the editor log:** dig designated on Regolith_A (60 t), then 2× SolarArray, BatteryBank, ChargePad, and the Forge — every order placed through the deck's click-to-place flow, every one crossed the 45 sim-s uplink and opened a construction site, **zero rejections**, all sites completed. Struct observed climbing (Forge batches) and dropping (construction consumption) simultaneously — the production/spending loop read correctly to the player. Sol autosaves fired throughout.
- **Director verdict: "controls are fine — functionally it's working."** Camera (WASD pan / wheel zoom / RMB-drag orbit / MMB-drag pan) and deck ship as the M1-a baseline. Director flagged gameplay + game UI as the axis to keep improving — folded into the M1-b plan (playability strand).
- **Gate C findings (queued M1-b, UI batch):** (1) the run stalled at the first step because the sim was paused and the only cue is the small `PAUSED` readout text — needs a prominent paused treatment; (2) placement/dig/confirmation prompts (`ShowToast` ghost/transmit messages) render under the help box exactly like the rejection toasts we already fixed — route ALL player-facing prompts through the deck's Slate notice channel and retire the GEngine debug path for gameplay feedback.
- **M1-a is closed:** Gates A (sim spine), B (StateTree brain), C (hands) all verified — the game is hand-playable end to end. Next: M1-b working spec (Z-model + fleet reality + inspection card), agent-proposed, awaiting director OK.

## 2026-07-06 — Session 12b (reel v3: the full quota→launch→landing arc, verified on footage)

- **Recording #4 (~17 min) diagnosis — reel v2 pacing bug, not a sim bug:** Pylon #2's order arrived 65 s before Pylon #1 completed (the Delay-150 estimate ignored robot workforce contention — the Forge's 400 fab-s hogged the crew — and blackout-frozen construction; pylon 1 actually completed ~1860 sim-s after its order). Rejected on link range → IceDrill/WaterPlant rejected outside coverage → no Water/Oxygen → Quota Q1 (needs Struct AND Water AND Oxygen) never met → `RH.Manifest`/`RH.Launch` silently no-op'd for 7 sols. Second find: the rejection toast **provably never rendered** (frame 0.7 s after the log line, blank screen) — `AddOnScreenDebugMessage` draws under the deck's opaque Slate help box. Third: `RH.Showcase` had been run manually in all three demo sessions, doubling idle load and overlapping the reel's Electrolyzer — demo runs are now explicitly hands-off.
- **Fix batch:** reel **v3** (pylon-2 beat 150→270 s, ice beat 90→150 s, manifest/launch windows 3→5, on-screen hands-off banner); **deck notice line** — the visualizer keeps the last rejection (`GetNoticeText`, 12 s real-time hold so it survives pause and 8×) and the deck polls it into an orange line under the colony readout: the visible rejection channel. Robot night-glow bump (0.6/0.5/0.28) went live in the same compile (21 s clean, cold).
- **Recording #5 (21.5 min, fresh Play, zero console commands — log confirms): the full M0 arc on camera for the first time, zero rejections.** Pylon chain held at the new beats; Water flowing by sol 4.5 (270/200 while Electrolyzer still building), Oxygen 89/50 by sol 5.5. `=== CEO TRANSMISSION (Sol 5): Quota Q1 met ON TIME ===` — same sol as the M0-c scripted run and the Gate B parity run, now emergent under the reel's timings. Green CEO toast + `QUOTA MET - manifest 0/10000 kg` readout line legible on camera; first manifest window composed 2300 kg and launched; `SUPPLY SHIP INBOUND` readout; `=== SHIP LANDED (Sol 8) ===` with the cyan landing toast, the ship visual, and the cargo build-credits appearing in the palette (SolarArray [2]→[3], BatteryBank [1]→[2]). Readout growing quota→manifest→inbound→continues lines end-to-end.
- **Observations:** once ISRU runs 24/7 the colony lives in permanent deficit (night load ~2005 W vs gen 50 W; SHED:9–10 every night; bank pinned near 0/6000) — quota timing unaffected since ISRU, not power, is the critical path; same brownout-throttling read as Session 9, still worth a UI treatment later. Robot night glow **unverified** — the static demo camera never catches the crew at night (they're at the ice field off-frame). Notice line **unexercised** this run (zero rejections — the good problem); stays as the safety net. Sim opens at sol 0 ~25% with 140 kg starter Struct — consistent across runs.

## 2026-07-06 — Session 12 (demo reel + recording-review fix batch — verified on director footage)

- **Demo pipeline root cause + fix:** recording #2 showed "no movement but the sun" because the `BP_DemoReel_Driver` had been placed in a deliberately-unsaved level — quitting the editor for the compile discarded it, so Play had no driver. Re-placed and **saved into L_Slice** (OFPA external actor; `save_actor` failed on the fresh package, `save_assets []` = Save All worked). Rule: demo drivers get saved, then removed by commit when retired.
- **Fix batch (compiled clean, verified against recording #3 — 163 s, one full sol):**
  - Construction sites: 4 hazard corner posts + mast work lamp — verified legible day AND night; the site→completed swap reads clearly in footage.
  - Era refusal now restores `LastAgentSpeed` + broadcasts a toast instead of silently dumping to 1× — compiled in, **still unexercised** (the one 60× press on record legitimately engaged era: nothing was under construction).
  - Robot night glow: 0.1 emissive proved invisible in footage → bumped to (0.6, 0.5, 0.28) this commit, **pending next compile + footage check**.
- **Reel v2 first act verified from the sim log:** 9 uplink orders in one burst, every one executed at exactly +45 sim-s; fabrication times per DataTable (Solar 60 s, ChargePad 120 s, Forge 400 s, Pylon 90 s); Forge batching 25 kg Struct per 100 sim-s; the Delay-150 pylon beat fired on time. Manifest/launch act not reached — director stopped at ~163 s; the full arc needs the ~12-min run.
- **Accidental stress test, passed:** `RH.Showcase` was run pre-recording (double idle load). Overnight the bank hit 0/10000 → **SHED:13** in the readout, construction froze, and dawn restored loads + resumed building with no errors — the load-shed system's first end-to-end showing in footage. The sol-1 autosave fired mid-recording (slot 'auto', 19 buildings, 7 robots).
- Notes for later: era→agent speed drop dumped 43 sim-s with a warning (accumulator flush; fold into M1-c era work); showcase IceDrill correctly warns "no deposit within 12 m"; SolarArray glass glow reads oddly bright at night (helps legibility; art-pass item).

## 2026-07-06 — Session 11 (Gate C visual rounds: designed gray-box — committed `ed1b71e`)

- **Director-driven iteration, screenshot-verified each round** (director played + screenshotted; agent diagnosed + fixed + compiled on editor-close gates — 6 compile cycles, all clean, ~13 s each once warm):
  1. **Upside-down/mirrored ground labels** — root cause: the label rotation was an Euler combo that faced the TextRender's front INTO the ground (camera read the back: mirrored + flipped). Diagnosed with two Simulate-mode captures (face fix vs in-plane flip are separate errors); final fix `MakeFromXZ(Up, +Forward)` — a pure rotation, never a reflection.
  2. **"No detail" round** — greeble: Forge chimney/slag-chute/catwalk, IceDrill 4-strut derrick + spoil auger, WaterPlant header pipe + valve wheels, BatteryBank rack rails + coolant fins, Electrolyzer manifold; SolarArray rebuilt as framed PV panel (mullions, A-frame mount, faint blue glass glow).
  3. **"No texture" round (material, MCP-only)** — `M_Graybox` gained metallic 0.6 + noise-varied roughness, then world-aligned **panel seams** (1.2 m grid, Frac→|d|→smoothstep, darkened into BaseColor), **per-panel value variation** (VNF_CellnoiseALU on the same grid), and a **dirt-skirt** Z-gradient at hull bases — all gated by a `SeamStrength` scalar so `MI_MarsGround` (SeamStrength=0, Metallic=0) keeps clean regolith. Deposit slabs keep the grid: reads as survey marking, kept deliberately.
  4. **"Bland and boxy" round (composition)** — foundation **plinths** under every structure; **50 cm saturated function-color band** per machine (emissive floor ×0.9 so hue survives midday exposure); **Lander rebuilt as a descent stage** (hull 60% of footprint on splayed legs + footpads, engine bell + throat ember, propellant spheres, capsule + dish — proportion, not decoration, is what killed the table-on-stilts read); **Electrolyzer rebuilt** as slim hall + two big domed flank tanks on cradles; **WaterPlant** block shrunk so the tank farm dominates; **Forge control annex** with lit crew windows; **ComputeModule** as dark server tower (status strips, cooling collar, uplink dish); BoneWhite darkened 0.62→0.52 for ground separation.
- **`RH.Showcase`** (`RHCheats` + `URHSimWorldSubsystem::Debug_Showcase`): one completed instance of every building type on a grid, bypassing the economy — the visual-QA harness for every round above, and for the underground camera work to come.
- **Toolchain lessons (memory updated):** (a) MCP viewport capture reads the editor world, NOT the PIE/Simulate world the director sees — showcase verification runs through the director's screenshots; (b) **in-memory material edits die with the editor session — `save_assets` after every MCP wiring batch** (one full node graph was lost to an editor restart and rebuilt); (c) external-drive disconnects mid-session reset the shell cwd and can zombie the build — verify volume + port 8000 before every compile; a build against an open editor emits a `-0001` hot-patch dylib that the next cold launch ignores — always relink clean after the editor closes.
- **Director ruling:** ship this as the Gate C look and move on; a deeper bespoke art/design pass on the buildings is **deferred to a later milestone** ("come back… when it makes more sense to improve our art design"). Logged in design-decisions.
- Committed `ed1b71e`. **Still open to close M1-a: the Gate C daylight hand-played sign-off** (fresh PIE, no showcase: dig → power spine → Forge → 8× → Struct climbing, camera/deck verdict), then the gate log.

## 2026-07-05/06 — Session 10 (M1-a Gate B: StateTree robot brain — verified, behavior-identical)

- **Architecture landed as specced (§5b):** the robot brain is now a StateTree asset (`/Game/RedHope/AI/ST_RobotBrain`) — Work + Charge states, `FRHNeedsChargeCondition` on the Work→Charge transition — with the tasks calling the exact sim API the M0-c switch called. `URHRobotBrainProcessor` ticks each robot's tree once per fixed sim sub-step (MassAI's plumbing, never its frame/signal scheduling: determinism preserved). Legacy switch processor stays live for robots without the ST fragment; `RH.BrainMode 0` forces it — two brains, one driver, permanent A/B instrument.
- **The tree asset is authored in code** (`RH.BuildRobotStateTree`, WITH_EDITOR): states, tasks, transitions, condition, compiled via `FStateTreeCompiler` — reproducible source, no GUI state. Toolchain trap #1: **assets created by console commands while Simulate runs inherit the PIE package flag and every save path silently skips them** — fixed with a duplicate → delete → rename-back launder through clean packages (no recompile needed).
- **The silent parity failure, and the 5.8 lesson that cost a day-part:** first ST run — robots inert, zero errors, zero logs. Diagnosis chain: probes showed the processor constructed + queries configured but `Execute` never called; the A/B run proved legacy fine; headless probes localized it. **Root cause (verified in engine source): the sync `CreateEntity(FragmentInstanceList, SharedValues)` derives its archetype with an EMPTY const-shared bitset and `InternalBuildEntity` never reconciles it** (only the async path upgrades via `GetOrCreateSuitableArchetype`). Our robots' archetype lacked the `FMassStateTreeSharedFragment` bit → the query's `AddConstSharedRequirement` matched zero archetypes → **Mass prunes processors with no matching archetypes, so Execute is never even called.** Fix: build the ST archetype composition explicitly (fragments + engine ActivatedTag + const-shared bit), spawn against it, set fragment data directly. Second engine trap defused en route: MassAI's activation processor would have **re-allocated our instance data and Started trees on wall-clock cadence** — robots pre-carry `FMassStateTreeActivatedTag` so the engine machinery never sees them; dynamic ST processors disabled by config.
- **New workflow discovered under pressure — the editor-free verification loop:** `UnrealEditor-Cmd <proj> <map> -game -nullrhi -stdout -FullStdOutLogOutput` runs the real game world headless (Mass phases, subsystems, fleet spawn all live). The archetype fix was verified this way — 7/7 robots matched, trees Running, Work ticking — before the editor ever reopened. Compile-and-verify without a director gate; this changes our iteration economics.
- **Parity verdict (BP_M0cTest re-run, StateTree brains, zero manual input):**
  - `=== CEO TRANSMISSION (Sol 5): Quota Q1 met ON TIME ===` — **same sol as the legacy run.**
  - `=== SHIP LANDED (Sol 9). Cargo transfer: 6 items, 5100 kg ===` — identical manifest, all cargo effects live, lag 45→8 s.
  - `=== THE PROGRAM CONTINUES: sols elapsed 9 | quota met sol 5 (deadline 10) ===` — **end card identical word-for-word.** Zero rejections, zero tree-start failures.
  - The matched-population signal ticked 7→**8** when the manifest's RC-E2 spawned — manifest-spawned robots join the StateTree brain mid-run.
  - Power numbers ran healthier than the M0-c original (bank 9054/10000 at sol 1 vs 0/10000) — that delta is the two director-approved Gate A balance fixes, not the brain; quota timing held regardless because the ISRU build-out, not power, is the critical path.
- Gate B closed. Next: **Gate C — hands** (camera/input wiring + Slate command deck v1; the first hand-playable build).

## 2026-07-05 — Session 9 (M1-a Gate A: save/load, era mode, CostResources, balance fixes — verified)

- **Compile:** 2 iterations (one missing forward declaration of `FRHRobotRow`); 21.5 s. One zombie-editor quit killed per the established procedure (hung in-editor shell held port 8000).
- **Headless proof (editor closed):** `UnrealEditor-Cmd -run=RHSim -sols=20` booted a bare Game world, brought the colony online (Lander, 7-robot fleet, dig designation through the 45 s uplink), era-integrated 20 sols in ~0.1 s CPU, printed the ledger, exited clean. The standing-order "headless + faster than real time" requirement is now demonstrated, not promised. Honest gap observed: with no production buildings, era logistics extracts nothing (it digs only to feed demanding consumers) where the agent band would stockpile raw regolith — quantified below, harness in M1-c.
- **DataTable path change:** delete+reimport failed this time ("already exists" after a true-returning delete) — but recompiled row structs already give live DTs the new columns, so `set_rows` wrote the new values directly (verified by read-back). Simpler than delete+import; adopted for column-addition cases.
- **Gate A SIE run (`BP_M1aTest`, ~10 min at 8×/60×, zero warnings or errors):**
  - **Save/load equivalence — exact.** Saved `siteA` at sol 0.98 mid-construction (stockpile site open, 1 task) mid-batch, ran +400 sim-s to a control checkpoint C1, loaded, re-ran the same span: C2 = `Sol 1 31% | gen 398 W load 835 W battery 0/6000 | Struct 170 | tasks 0` — **identical to C1 line for line.** The replay leg even re-fired the same sol-1 autosave.
  - **Balance fixes visible in the wild:** night status shows `gen 50 W` (Lander RTG trickle, was 0 in every M0 run); battery bank completion credits capacity AND the half-charge (344/6000 pre-sol-1).
  - **Era mode:** engaged at 60× — 0.4 sol integrated in 8 real seconds mid-run, then a full sol in 20 s on the divergence leg; autosaves fire at era sol boundaries; agent band resumes cleanly at 8× after. Auto-drop guard present but untriggered this run (construction had finished before each engage; the refusal path gets exercised in M1-c's storm auto-drop).
  - **Divergence sample (agent vs era, 1 sol from the same `clean` save):** Struct 570 (agent) vs 520 (era) — **8.8% divergence, above the proposed 5% bar.** Cause identified: era steps decrement batches in 1-min quanta with no overshoot carry, so a 100-sim-s Forge batch takes 120 era-sim-s (~17% slower cycling). Fix (batch overshoot carry) queued for the M1-c paired-run harness rather than patched blind mid-gate.
  - **CostResources:** every build this run parsed the new bill-of-materials column (all rows migrated `CostStruct_kg`→`CostResources`); the multi-line case arrives with M1-d's Habitat.
  - Emergent observation: a colony in sustained power deficit (bank 0, load 835 W vs ~400 W day gen) duty-cycles via shed oscillation — the Forge browns out to ~50% throughput instead of stalling. Reads as brownout throttling; acceptable behavior, worth a UI treatment later.
- `BP_M1aTest` kept as the Gate A regression harness (not placed). Gate B next: StateTree robot brain — needs the MassAI plugin enabled (director OK + restart, batched with the Gate B compile).

## 2026-07-05 — Session 8 (M0-c: the slice finale — charging, site delivery, shedding, ISRU, quota→ship)

- **Systems (committed `0f1fefc`, compiled clean):** robot charging etiquette (break off below 25% battery — unless mid-delivery — release the claim, dock at the nearest completed pad, draw the pad's 500 W from bank-then-live-surplus via `RequestChargeWh`, resume at 90%; both fractions are `DT_Config` rows; **no pad built = the robot works until it dies where it stands, deliberately** — that lesson belongs to the player). Construction-site material delivery (task board posts Struct hauls from any completed holder to the site; the fabricator stands by until the full load is delivered; the material is consumed into the structure at completion — replaces M0-b's instant colony-wide deduction). Priority load-shedding (bank empty + load > gen → shed lowest `LoadPriority` first until demand fits generation: Forge first, pads last; `SHED:n` in `RH.Status`). Extraction recipes (empty `Inputs` + `RequiresDeposit` → the batch draws its output mass from the deposit the building auto-attached to at placement; `DrillIce` row added). Pylon link-range rule (defs with coverage+link place by ≤80 m link to the nearest *completed* node; everything else still needs coverage). Quota state machine (Open → CEO transmission with on-time/late award by met-sol → manifest composition against the mass budget → launch → 3-sol transit → arrival applies cargo effects → end card), `RH.Manifest`/`RH.Launch` commands, quota toast + gray-box supply ship in the visualizer.
- **Smoke test (SIE, one scripted ~38-min run at 8×, `BP_M0cTest`, authentic 45 s uplink lag — no test shortcut): the entire M0 loop ran end-to-end with zero order rejections and zero manual interventions.**
  - Sol 0–1: pad, 6 solar, 2 banks, stockpile, Forge built from the 400 kg starter Struct via site deliveries; dig designated on Regolith_A.
  - Sol 1.5 / 2.7: pylons at (55,55) and (110,110), each accepted on link range to the previously *completed* node.
  - Sol 4–5.3: IceDrill on Ice_A (auto-attached), WaterPlant beside it in pylon coverage, Electrolyzer at base. The drill ran `→Ice:40` extraction batches immediately; the one-rule output router sent drill ice to the plant (`Ice:50→Water:45`) and fluids to the network pool — the full ISRU chain came alive with no special-case code.
  - **Sol 5: `=== CEO TRANSMISSION (Sol 5): Quota Q1 met ON TIME ===`** (deadline 10; 10,000 kg budget). Forge held ~170 kg Struct/sol; fluids filled within ~a sol of the chain going live (final: Struct 915/300, Water 1550/200, O₂ 454/50).
  - Sol 7: scripted manifest — SolarPack, BatteryPack, PartsCrate, Toolkit, ComputeCore, AdvExcavator = 5,100/10,000 kg — `Ship launched: 6 items, arrival in 3 sols`.
  - **Sol 9 (99%): `=== SHIP LANDED. Cargo transfer: 6 items, 5100 kg ===`** — every cargo effect applied live: import stock +1 solar +1 battery (visible in the next status), +10 SpareParts, fabricator speed ×1.15, `Compute core online: order lag now 8 sim-s` (45→8), and the RC-E2 spawned into the Mass fleet. **`=== THE PROGRAM CONTINUES: sols elapsed 9 | quota met sol 5 (deadline 10) | fleet + colony operational ===`** Post-launch manifest attempts all bounced with "No manifest is open" — phase enforcement demonstrated by the script's own redundant attempts.
- **Shedding evidence:** midnight statuses read `gen 0 W load 0 W battery 0/10000 Wh SHED:13..17` once the colony grew — at night on an empty bank the entire grid sheds, pads included: docked robots wait for sunrise (`RequestChargeWh` returns 0). Reads as intended drama; whether the Lander should hold a pad reserve is an M1 balance question.
- **Charging-etiquette evidence:** an RC-E runs ~0.75 sol on a full battery; the fleet stayed operational 14+ sols with fresh dig claims every sol — roughly a dozen seek→dock→resume cycles per excavator, including shed-night waits docked at the pad.
- Captures: `docs/media/m0c-colony-sol4.png` (night, chain going up), `m0c-ship-landed.png` (dawn, supply ship on the pad), `m0c-isru-chain.png` (drill + plant on the ice, pylon line back to base).
- **End-of-run benchmark** (`RH.Benchmark m0c_end`, sol 14, 17 buildings + 8 task-driven robots + full production/power sim at 8×, in-editor SIE): avg 41.19 ms (24 fps), p95 42.80 ms, worst 48.95 ms, mem 6,420 MB — at or under the empty-world scaffold baseline (46 ms). The complete slice sim is unmeasurable against the editor render floor; the M2–M4 population headroom conclusion stands.
- **Known issues logged, deliberately not fixed mid-verification:** (1) battery packs' arrive-half-charged credit is clamped away while the bank is under construction (capacity only counts completed storage) — move the credit to construction completion (M1); (2) `RH_Buildings` notes say the Lander "trickle gens" but generation multiplies by the solar curve → 0 at night; reconcile note vs sim in the M1 balance pass; (3) manifest cargo effects are name-keyed in code at slice scale — data-driven effect verbs are the logged M1 task; (4) hand-placed `GB_Dep_*` markers in L_Slice now duplicate the sim-spawned deposit visuals — cleanup queued.
- **M0 exit: all three stages verified in-engine (a: power/territory · b: production spine · c: finale).** The vertical-slice loop — dig → smelt → extend the grid → ISRU → quota → manifest → ship — is complete and self-sustaining. `BP_M0cTest` kept as the regression harness (not placed in the level). Stopped for director review.

## 2026-07-05 — Session 7 (M0-b: production spine — dig/haul/build/smelt, quota)

- **Systems (compiled in 2 passes; 1 header omission fixed):** sim world is now the single sub-step driver (uplink → task board → production → power). Deposits load from `DT_Deposits`; `RH.Dig` designates through the uplink. Task board posts haul work by recipe demand and build work from timed construction. Robots are task-driven Mass agents (`FRHRobotFragment`/`FRHTaskFragment`, claim→travel→work→deliver in `URHRobotTaskProcessor`); starting fleet spawns from `DT_Robots.StartingCount` on the first sim step. Struct costs deduct colony-wide; import-only hardware consumes starter flat-pack stock. Production runs recipes on the hybrid logistics split (solids in hoppers, fluids to the network pool); batches stall on power deficit; active-vs-idle draw feeds the ledger. Quota progress vs Q1 in `RH.Status`.
- **Smoke test 1 (unpowered, scripted BP_M0bTest):** the full chain ran — fleet ×7, designation, dig at exactly 2×50 kg/h (128 kg per 64 sim-s measured), haulers skimming piles to the Lander, Forge cost 250 kg (400→150), fabricator worked the 400 s site down, hopper fed, batch started — **then the colony hit `gen 47 W load 820 W battery 0 DEFICIT` and the batch froze at 0.82 h remaining.** The test built no solar; the deficit-stalls-production rule fired exactly as specced. "Every watt matters" emerged un-scripted in the first run.
- **Smoke test 2 (powered, BP_M0bTest2):** 2 solar + battery bank + Forge through the uplink; fabricator built all four sites serially; battery cap 2000→6000 Wh; sunrise charging (1879→4788 Wh); Forge fed and **self-sustaining: 4+ batches completed and auto-restarted (Regolith:100 → Struct:25 each), quota Struct 150→225/300 kg and climbing**, with the 825 W load bridged by the bank against the falling afternoon curve — no deficit.
- Capture: `docs/media/m0b-colony-running.png` (soft — TAA never converges at 8× sim speed; flagged for the look pass). BP_M0bTest/BP_M0bTest2 kept as regression harnesses.
- Known M0-c scope: robot charging etiquette (drained robots currently halt for good), material delivery to construction sites, load-shedding by priority, ISRU chain smoke test (drill/plant/electrolyzer), quota completion → manifest → ship arrival.

## 2026-07-05 — Session 6 (Mars sky pass + M0-a: definitions, power, territory)

- **Mars sky (director directive, rover-referenced):** VolumetricCloud + template SkySphere deleted (no clouds on Mars). SkyAtmosphere inverted from Earth: thin warm Rayleigh (butterscotch dust dome), cool high-anisotropy Mie lobe (blue sunset aureole per rover photos), reddish ground albedo, warm dust fog, 0.357° solar disk at 7 lux. Captures: `docs/media/mars-sky-midday.png`, `mars-sky-sunset.png`. Logged as the habitability-0 endpoints for the dial curves.
- **M0-a systems (compiled clean, 22.5 s, first try):** `URHDefinitionsSubsystem` (DT/CT registry; sole owner of `CanFabricateLocally`); sim world rewritten as tickable colony owner — data-driven buildings, power ledger (gen × `CT_SolarDiurnal`, idle loads, battery Wh clamp, deficit flag), coverage-disc territory with Build rejection, uplink queue pumped at sub-step boundaries; clock refactored to published per-frame step counts (multi-reader timeline; single-driver refactor queued for M0-b); colony visualizer (gray boxes from sim events + coverage disc debug draw + on-screen rejection toasts); console commands `RH.Build` / `RH.Status` / `RH.Lag`. Hand-placed gray-box colony + StressDriver removed from L_Slice (sim spawns the colony; BP kept as benchmark harness).
- **M0-a smoke test (SIE, scripted via BP_M0aTest — kept as regression harness):** all six checks passed in one run:
  1. Bootstrap: Lander #1 at (0,0); lag 45 sim-s from DT_Config; sol length 1200.
  2. Night power: gen 0 W at sol 0–17%, battery draining at exactly load × sol-hour rate (23 Wh over 48 sim-s at ~25 W — units doctrine verified).
  3. Uplink: Build Pylon queued visibly ("executes at t=56, now 27"), landed after 45 sim-s.
  4. Territory chaining: SolarArray at (50,0) accepted only because the new pylon's 40 m disc covers it.
  5. Territory rejection: Stockpile at (200,0) traveled the 45 s uplink and was rejected on arrival — "Outside grid coverage - extend pylons first."
  6. Sunrise: at sol 37%, gen 278 W (curve × panel + lander trickle), battery charging 889→1256 Wh.
- Next (M0-b): single sim driver ordering, Struct costs + build time, dig/haul/production loops, robots as task-driven agents, deposit visuals from data, quota tracking.

## 2026-07-05 — Session 5 (Step 4 scaffold, part 2: content, benchmark, smoke test)

- **Content built via MCP:** 8 DataTables imported from `docs/data/RH_*.csv` against the compiled `FRH*Row` structs (row values spot-verified; `ImportOnly`/`UnlockTech` live). `CT_SolarDiurnal` (diurnal curve), `CT_AtmosphereDial` (FogDensity, SunIntensityMul), `CT_CameraRig` (Distance/Pitch/FOV) populated. `MPC_Atmosphere` (Habitability/TimeOfSol/DustAmount/SkyTint) + `M_MarsDial` MPC-driven material. Sun tagged `RH.Sun`.
- **Gray-box L_Slice:** Mars ground plane (5×5 km) + lander, 3 solar arrays, battery, pylon, charge pad, Forge, stockpile, 6 deposit markers at `RH_Deposits` coordinates. **Gotcha:** duplicating the engine OpenWorld template does not bring its World Partition landscape proxies (external actors stay under `/Engine`); the orphaned Landscape actor was removed and a plane used instead — real terrain is an M0 map-pass task.
- **Console-exec gap workaround:** MCP has no console-command tool, so `BP_StressDriver` (authored via the Blueprint graph DSL) fires the benchmark sequence from BeginPlay: baseline → 200 agents → 8× → 500 agents → 8×, via `RH.SpawnDummies` / `RH.SetSpeed` / `RH.Benchmark`.
- **Hardware benchmark (director-required) — PIE Simulate, in-editor viewport, Lumen defaults, this Mac (arm64):**

  | Phase | avg ms | fps | p95 ms | worst ms | memory |
  |---|---|---|---|---|---|
  | 0 agents, 1× (baseline) | 46.29 | 22 | 50.73 | 58.08 | 6,214 MB |
  | 200 agents, 1× | 46.73 | 21 | 51.51 | 56.95 | 6,217 MB |
  | 200 agents, 8× | 46.91 | 21 | 51.44 | 58.75 | 6,222 MB |
  | 500 agents, 1× | 47.77 | 21 | 52.47 | 202.82 | 6,233 MB |
  | 500 agents, 8× | 47.91 | 21 | 88.67 | 190.05 | 7,342 MB |

  **Reading:** agent cost is in the noise — +1.6 ms at 500 agents × 8× sub-stepping (≈4,000 agent-updates/frame-equivalent) over an empty world. The 46 ms floor is the in-editor Lumen viewport on this hardware, not the sim. Memory +19 MB for 500 agents+instances (the 1.1 GB jump in the last phase coincides with the HighResShot capture, not agents). The p95/worst spikes at 500 appear at capture/GC moments. **Conclusion: population targets for M2–M4 (hundreds of agents) are not hardware-limited on this machine; the budget pressure is rendering quality settings, which are tunable.** Numbers are in-editor Simulate — packaged builds will run faster; deltas are the architecture signal.
- **Smoke test:** PIE-Simulate ran the full 95 s sequence unattended; sim clock speed tiers exercised (1×/8×); Mass wander processor + battery drain sub-stepping live; ISM visualizer tracked 200→500 entities; atmosphere subsystem wrote MPC + drove the tagged sun. Captures: `docs/media/scaffold-graybox-editor.png` (first layout), `scaffold-graybox-grounded.png` (grounded pass), `scaffold-sie-500-agents.png` (live Simulate, 500 agents).
- Editor sky is still Earth-blue (template sky rig); Mars-ifying the sky/fog via the dial curves is the M0 look pass, deliberately not scaffold scope.

## 2026-07-05 — Session 4 (Step 4 scaffold, part 1: repo, sources, template removal, first compile)

- `git init` (branch main); baseline commit `557d902` = pristine pre-scaffold restore point (director requirement: lands before any deletion).
- Wrote Source tree: `RedHopeSim` (sim clock w/ fixed 0.1 s sub-steps + speed tiers, sim-world subsystem w/ stocks ledger + uplink latency queue, Mass fragments + dummy-agent spawner, wander processor with per-sub-step integration, DataTable row structs incl. `ImportOnly`+`UnlockTech` per directive, commandlet stub) and `RedHope` (GameMode/GameState/PC, strategy pawn w/ exponential zoom mapping, atmosphere subsystem [MPC writer + `RH.Sun`-tagged sun driver + `RH.Habitability` scrub CVar], ISM agent visualizer, `RH.SpawnDummies`/`RH.SetSpeed`/`RH.Benchmark` console commands).
- All Mass API usage verified against the 5.8 engine headers before compiling. 5.8 restructured Mass: **MassCore is engine-core** (`Runtime/Mass/MassCore`, owns `FTransformFragment` + `FMassEntityHandle`); `ConfigureQueries` now takes `TSharedRef<FMassEntityManager>`; `ForEachEntityChunk` dropped its EntityManager arg; the **processing-phase ticker (`MassSimulationSubsystem`) is in the MassGameplay plugin** — hence MassGameplay is required for agents to tick at all.
- Director-approved deletions executed with `L_Slice` open (never the open level): `Content/TopDown`, `Characters`, `LevelPrototyping`, `Cursor`, and their `__ExternalActors__`/`__ExternalObjects__` subfolders. `L_Slice` created by duplicating the engine OpenWorld template (World Partition) — MCP has no new-level tool. Config retargeted (startup/default map → `L_Slice`, GlobalDefaultGameMode → `/Script/RedHope.RHGameMode`).
- `.uproject`: added Modules (RedHope, RedHopeSim), enabled MassGameplay (director-approved, one restart).
- Session moved from the in-editor Terminal plugin to an external terminal for the build (editor quit kills in-editor sessions; `.mcp.json` → `http://127.0.0.1:8000/mcp` reconnects externally).
- **Compile: success** (red_hopeEditor, Development, arm64; 32 s; 2 iterations). Gotcha for the record: `FMassEntityHandle` needed an explicit `#include "Mass/EntityHandle.h"` — not reliably visible via `MassEntityTypes.h` in a fresh TU. `ExecutionFlags` is `uint8` in 5.8.
- Next (pending director reopening the editor): CSV→DataTable imports, MPC_Atmosphere + curves + sky rig, gray-box slice map, 200/500-agent benchmark @1×/8× (frame ms + memory, logged here), PIE-Simulate smoke test + captures.

## 2026-07-04 — Session 3 (Step 3: architecture + toolchain stress tests)

- Logged Step 2 approval + import-only-is-removable directive in design-decisions.md.
- Stress-tested MCP toolchain in-editor (all artifacts under /Game/RedHope_StressTest, deleted after):
  - MPC pipeline verified end-to-end: created MPC_DialTest, wrote scalar+vector params via ObjectTools (auto-GUIDs confirmed via round-trip read), created M_DialTest, added CollectionParameter expression, bound to collection + wired to BaseColor, recompiled clean. **The atmosphere-dial backbone is fully MCP-buildable.**
  - CurveTable created + solar diurnal keys set/verified. CSV import path exists.
  - DataTable create works but requires existing C++ FTableRowBase structs (no struct-creation tool; registry shows engine structs only) → workflow: agent writes C++ structs → director compiles → agent imports CSVs.
  - Blueprint create (arbitrary parent) + compile verified; graph DSL available for BP logic.
  - BlueprintTools describe overflowed context limits; worked from saved-file extraction.
- Engine facts verified: MassEntity is engine-core in 5.8 (Engine/Source/Runtime/MassEntity); MassGameplay + MassAI plugins present but not enabled; GameplayStateTree already enabled in uproject.
- Delivered `docs/ue-architecture-proposal.md`. Stopped for director review. Editor left clean (stress-test folder deleted).

## 2026-07-04 — Session 2 (Step 2: M0 spec)

- Logged director approvals in `docs/design-decisions.md`: all eight §9 recommendations, map model (formally the ninth question), hybrid logistics, Hope as derived index, silent identity-axis accumulation.
- Delivered M0 vertical-slice spec: `docs/m0-vertical-slice-spec.md` — units/clock doctrine, two-timescale model (agent band 1×/3×/8× + specced 60× era-mode aggregate integrator), expansion-headroom decision (World Partition + authored sectors through v1), power/territory model, 5 robot types, Forge and ISRU chains, quota Q1, manifest catalog, slice map, confidence flags.
- Authored 9 DataTable-ready CSVs under `docs/data/` (config, resources, buildings, recipes, robots, manifest items, quotas, deposits, solar diurnal curve). Dormant M1+ rows included so later milestones activate content, not schema.
- Stopped for director review. No editor/project assets touched this session.

## 2026-07-04 — Session 1 (kickoff)

- Verified Unreal MCP toolchain: 20 toolsets enumerated and reachable. Engine confirmed UE 5.8.0 (Release-5.8, CL 55116800). Editor live with `/Game/TopDown/Lvl_TopDown` open.
- Round-trip test: spawned a StaticMeshActor `MCP_Handshake_DeleteMe` into the level via MCP, then deleted it. Level not saved; no persistent change.
- Design brief was **not present in the project tree**; located at `~/Downloads/files/the-red-hope-design-brief.md` and copied to `docs/the-red-hope-design-brief.md`.
- Seeded `docs/design-decisions.md` and this build log.
- Delivered Step 1 (digest, gaps, open-question recommendations) — archived at `docs/step1-digest-and-recommendations.md`. Stopped for director review per working rules.
- Noted: project is not a git repository. Recommended `git init` before any scaffold work (Step 4).
- No project assets or settings were modified this session.
